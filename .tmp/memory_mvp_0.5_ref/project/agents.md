# Project Agent Rules

Цей файл містить project-specific адаптації `memory/reglament/agents.md`.

## Поточні адаптації

- Немає. Використовуються базові правила PDADM MVP 0.5.

## Правило оновлення

- Додавати тільки правила, специфічні для цього проекту.
- Явно вказувати, яке базове правило адаптується й у якому scope.
- Не дублювати весь `reglament/agents.md`.
- Змістові зміни цього файла готувати через `FIX-*`.
