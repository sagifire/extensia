# Fixation: FIX-001

Status: applied
Requirement: required
Related Task: [P4-VS3 / TASK-07.26-0052](task.md)
Related Run: [RUN-002](RUN-002/index.md)
Related Research: none
Created: 2026-07-17
Review Status: initial implementation audit `REVIEW_READY`; post-application audit `CHANGES_REQUIRED` через pre-existing Asset-contract ownership ambiguity поза approved scope, correction proposed as FIX-002
Review Record: [RUN-002 result](RUN-002/result.md)

## Призначення

Після whole-task і separate fixation approval синхронізувати canonical current/product/technical memory з verified P4-VS3 internal Asset bytes lifecycle: exact opaque adapter, bounded SQLite-resident staging, atomic publish/discard, last-ready replacement semantics і recovery evidence. Не оголошувати ordinary public upload/file API, P4-STAB acceptance, universal platform/power-loss certificate, P5 indexes/sync або P7 freeze.

## Джерело рішення

- Frozen effective requirements та executable evidence `P4-VS3 / TASK-07.26-0052 / RUN-002`.
- Applied [Asset Contract](../../../technical/asset-contract.md), [Write/Journal/Recovery Contract](../../../technical/write-journal-recovery-contract.md) і accepted [ADR-0010](../../../technical/decisions/ADR-0010-local-sqlite-storage-protocol.md).
- Explicit human decision 2026-07-17: materialization exact opaque adapter є first deliverable цього самого vertical slice.

## Цільові файли

- `memory/domain/current/implementation-state.md`
- `memory/technical/architecture.md`
- `memory/technical/asset-contract.md`
- `memory/technical/write-journal-recovery-contract.md`
- `memory/technical/decisions/ADR-0010-local-sqlite-storage-protocol.md`
- `memory/technical/open-questions.md`
- `memory/product/roadmap.md`

## Запропоновані зміни

### `memory/domain/current/implementation-state.md`

1. Змінити `As Of` на `2026-07-17`.

2. Після P4-VS2 bullets додати:

```text
- `P4-VS3 / TASK-07.26-0052 / RUN-002` materialize-ила exact internal Core↔driver Asset upload capability як first deliverable того самого slice: adapter-scoped non-enumerable/non-serializable handles, resolve/issue/stage/inspect/read primitives, fixed 64 KiB chunks і 16 MiB payload limit. Deterministic fake та `local-sqlite-v1` реалізують один internal contract без public driver-author, path/session/SQL або package-subpath surface.
- Internal begin/resolve/stage/finish/abort/read lifecycle використовує той самий runtime-owned Operation Engine, identity source, Resource lock/index і driver semantic transaction. Stage є bounded invisible whole-payload transaction без journal; begin/finish/abort commit-ять metadata, exact payload action і рівно один journal row. Initial bytes не читаються до finish; replacement зберігає last-ready payload до atomic finish, а abort/incomplete/stale generation його не замінює.
- SQLite startup/integrity scan перевіряє payload length, SHA-256 digest, contiguous exact chunks і generation ownership. Process-crash/fault/restart matrix доводить rollback до COMMIT, recovery після durable COMMIT, last-ready preservation і cleanup warning semantics; staged bytes лишаються invisible. Evidence є bounded current-host/process proof, не destructive power-loss або universal-platform certificate.
- Measured current-host 16 MiB envelope: 256 chunks по 64 KiB; initial DB growth близько 16.86 MiB, successful stage/finish блокують event loop у bounded synchronous interval, а replacement finish rollback journal сягає близько 17.10 MiB. Exact sample і non-SLA limits зафіксовані в RUN-002 result.
```

3. У `Поточні гарантії` замінити твердження про відсутні bytes transport/finalization на current internal bounded P4-VS3 capability/lifecycle, залишивши ordinary public bytes/file API, P4-STAB, P5, P7, final/default public driver surface і broader/power-loss certification як deferred.

4. У `Межа current/target` додати internal opaque upload capability, stage/read, begin/finish/abort, atomic publish/discard та startup payload integrity до current. Залишити application-facing bytes transport/file read facade, streaming, external blobs, P4-STAB, P5/P7 й broader certification у target/deferred.

5. У `Джерела` додати:

```text
- `memory/tasks/plan/TASK-07.26-0052-p4-vs3-internal-asset-upload/RUN-002/result.md`.
```

### `memory/technical/architecture.md`

1. Змінити `Updated` на `2026-07-17`.

2. У `Статус реалізації` замінити P4-VS3 absence на bounded internal upload lifecycle implementation fact; ordinary public upload/file API, public/default concrete driver construction, P4-STAB і broader certification лишити deferred.

3. Після P4-VS2 paragraph додати:

```text
P4-VS3 materialize-ила storage-issued adapter-epoch-scoped opaque handle та один shared capability map для deterministic fake і `local-sqlite-v1`. Trusted non-root composition resolver передає internal Core upload port; root exports, ordinary `StorageFacade`, `AssetSnapshot` і public full-driver author contract не розширені. Stage копіює caller bytes, fixed-chunk materialize-ить їх invisible в окремій SQLite transaction без journal, а finish/abort/begin проходять чинний Operation Engine semantic commit. Driver-owned `generation.publish` atomically видаляє old committed payload за replacement, переносить staged chunks під Asset ID, видаляє generation і commit-ить metadata + exact `asset_changes` + one journal row в одній durability domain. Startup scan fail-close перевіряє digest/length/chunk continuity; old runtime handle не має authority після adapter reopen. Synchronous 16 MiB envelope виміряний, але не є SLA або universal support claim.
```

4. У SQLite storage section уточнити, що current `payloads`/`payload_chunks` seam тепер materialized для staged/committed Asset bytes без schema-version increment: upload IDs key-ять invisible staged rows, Asset IDs — committed rows, а generation row є atomic ownership bridge.

### `memory/technical/asset-contract.md`

1. У `10. Compatibility review / Current AssetSnapshot` замінити останнє речення про pending P4-VS3 на:

```text
`P4-VS3 / TASK-07.26-0052 / RUN-002` materialize-ила internal bytes/finalization contract без зміни public `AssetSnapshot`, root namespace або ordinary `StorageFacade`: opaque handle не enumerable/serializable і має adapter-open-epoch authority; public application bytes/file API лишається окремим bounded design, а P7 compatibility freeze не заявлений.
```

2. У downstream table змінити prerequisite P4-VS3 на `accepted P4-VS2 + explicit corrected RUN-002 activation`; deliverable доповнити exact adapter materialization first, bounded stage/read і startup payload integrity; gate proof доповнити pressure/package evidence.

3. Замінити downstream sentence про неможливість старту без pre-existing executable primitives на factual disposition:

```text
RUN-001 виявила circular ownership, бо predecessors лишили conceptual/inactive seam. Explicit human decision 2026-07-17 передало RUN-002 ownership materialize-ити exact executable adapter як first deliverable цього ж slice; consumer lifecycle підключено лише після shared fake/SQLite conformance, без parallel path або послаблення gate.
```

4. Normative lifecycle, transition, failure, journal і verification sections не переписувати: implementation відповідає вже applied contract.

### `memory/technical/write-journal-recovery-contract.md`

У `Concrete profile local-sqlite-v1` замінити `майбутніх opaque payload chunks` на current bounded opaque staged/committed payload chunks і додати:

```text
P4-VS3 stage bytes є invisible non-journal SQLite transaction, а begin/finish/abort лишаються semantic operations чинного Core/Operation Engine. Publish/discard/delete виконуються driver-owned compound actions тієї самої metadata+journal transaction; committed visibility ніколи не має independent payload commit authority. Startup recovery/integrity перевіряє payload digest, length, exact contiguous chunk set і generation ownership до ready.
```

### `memory/technical/decisions/ADR-0010-local-sqlite-storage-protocol.md`

1. Змінити `Updated` на `2026-07-17`.

2. У рішенні замінити `майбутні opaque payload chunks` на `bounded staged і committed opaque payload chunks`.

3. Після schema/version bullet додати:

```text
- P4-VS3 використовує current `user_version=2` без migration: `payloads`/`payload_chunks` key-яться opaque active upload ID до finish і Asset ID після publish. Stage виконується invisible whole-payload transaction без journal; publish/discard/delete є compound actions semantic metadata+journal transaction. Exact 64 KiB chunks, 16 MiB payload maximum, SHA-256/length/continuity scan і adapter-epoch handle authority є internal profile constraints, не public Storage Driver API.
```

4. У `Наслідки` додати P4-VS3 internal bytes lifecycle до implemented capabilities й залишити P4-STAB, public/default surface, broader performance/platform та destructive power-loss gates deferred.

### `memory/technical/open-questions.md`

1. Змінити `Updated` на `2026-07-17`.

2. У closed P4-DG1 baseline замінити bytes transport/finalization як open gate на accepted bounded P4-VS3 internal SQLite-resident lifecycle; open лишити ordinary public bytes/file API, streaming strategy, broader payload/performance/platform і destructive power-loss profiles.

3. У closed P4-DG2 baseline замінити `Still open` P4-VS3 adapter/lifecycle на completed RUN-002 fact; open лишити application-facing API design, P4-STAB, P5 indexes/sync і P7 freeze.

### `memory/product/roadmap.md`

1. Змінити metadata `Updated` на `2026-07-17`.

2. Замінити Phase 4 status на:

```text
Стан: P4-DG1/P4-DG2 approved/applied; P4-WP1 concrete driver, P4-VS1 Resource durability і P4-VS2 Asset metadata lifecycle accepted; P4-VS3 internal bounded upload bytes/finalization implementation review-ready, але ще не accepted. P4-STAB лишається pending і не активована.
```

3. У Phase 4 bullets відмітити implemented exact opaque adapter, internal begin/stage/finish/abort/read, last-ready semantics, atomic payload actions і bounded recovery/pressure/package evidence. Не називати Phase 4 done до whole-task acceptance P4-VS3 та наступного P4-STAB/human gate; ordinary public bytes API, broader platform/power-loss certification лишити deferred.

## Обгрунтування

Без fixation canonical memory продовжує стверджувати, що Asset bytes transport/finalization і executable adapter відсутні, а payload chunks є лише future seam. Proposal прибирає цю суперечність, одночасно не перетворює internal P4-VS3 port на public API й не розширює bounded process/current-host evidence до release certification.

## Вплив і узгодженість

- `state.md`: operational review-state update виконується без recursive fixation.
- Product roadmap: included; P4-VS3 стає review-ready, P4-STAB не активується.
- Domain current/target/rules: current included; target/rules normative semantics уже applied у P4-DG2 і не змінюються.
- Technical architecture/rules/ADR/contracts: architecture, Asset compatibility/downstream disposition, write/recovery current fact і ADR-0010 included; technical rules не потребують semantic зміни.
- Knowledge packages та project-specific rules: not-needed.
- Wiki indexes: not-needed; structural canonical artifacts не додаються.
- Language gate: passed; canonical author text український, identifiers retained in English.
- Architecture pressure: checked; capability приєднана до existing session через internal WeakMap, одна durability domain і один Operation Engine/journal/index authority збережені.

## Ризики

- Fixation не можна застосовувати без whole-task і окремого `FIX-001` approval.
- `resolveInternalAssetUploadPort`/`resolveLocalSqliteAssetUploadPort` є source-internal non-root composition seams; їх не можна описувати як package/public API.
- Whole-payload synchronous 16 MiB baseline не є streaming API, SLA або рекомендацією для більших payloads.
- Process-crash/fault evidence не є destructive power-loss certificate.
- Review-ready P4-VS3 не завершує Phase 4 й не активує P4-STAB автоматично.

---

## Approval

Status: approved
Reviewed: 2026-07-17
Approval Source: explicit user decision `FIX-001: approve`
Approval Notes: User separately approved whole TASK-0052 result and required FIX-001 application.

## Застосування

Applied: 2026-07-17
Applied Files: `memory/domain/current/implementation-state.md`; `memory/technical/architecture.md`; `memory/technical/asset-contract.md`; `memory/technical/write-journal-recovery-contract.md`; `memory/technical/decisions/ADR-0010-local-sqlite-storage-protocol.md`; `memory/technical/open-questions.md`; `memory/product/roadmap.md`
Verification: self-review, UTF-8 replacement-character scan і `git diff --check` passed. Independent post-application audit confirmed all seven target deltas materially accurate and boundaries preserved, але повернув `CHANGES_REQUIRED` через два pre-existing Asset-contract sentences, які approved FIX-001 прямо не дозволяла переписувати; minimal correction prepared as FIX-002.
Deviations From Approved Proposal: roadmap status одразу зафіксовано `accepted`, а не проміжне `review-ready, але ще не accepted`, бо тим самим explicit user message whole-task result уже був approved. Це lifecycle-only evolution після approval, без semantic scope expansion; P4-STAB лишається pending/not activated.
Result: approved FIX-001 applied; canonical current/product/technical memory synchronized with accepted P4-VS3. Closure awaits separate FIX-002 decision and repeated post-application audit.
