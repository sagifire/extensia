# Fixation: FIX-001

Status: applied
Requirement: required
Related Task: [P5-VS1 / TASK-07.26-0060](task.md)
Related Run: [RUN-001](RUN-001/index.md)
Related Research: none
Created: 2026-08-23
Review Status: approved, applied exactly і independently audited `PASS`
Review Record: [RUN-001 result](RUN-001/result.md)

## Призначення

Після whole-task і separate fixation approval синхронізувати canonical current/product/technical memory з фактичною accepted реалізацією P5-VS1: coherent `greedy`/`lazy` generation, complete-only existing reads, experimental descriptor-safe `readModel` config, explicit `query.refresh()` і safe synchronization inspection поверх одного hardened actor/coordinator. Не заявляти concrete SQLite polling, live multi-process support, notification transport або final P7 compatibility.

## Джерело рішення

- Frozen acceptance criteria й verified implementation `P5-VS1 / TASK-07.26-0060 / RUN-001`.
- Applied [Read-model completeness contract](../../../technical/read-model-completeness-contract.md) і [Multi-instance synchronization contract](../../../technical/multi-instance-synchronization-contract.md).
- Accepted P5-WP1 та P5-HARD1 foundations, які P5-VS1 інтегрує без другого actor, coordinator, journal або mutation path.

## Цільові файли

- `memory/domain/current/implementation-state.md`
- `memory/technical/architecture.md`
- `memory/technical/read-model-completeness-contract.md`
- `memory/technical/multi-instance-synchronization-contract.md`
- `memory/technical/public-read-contract.md`
- `memory/product/roadmap.md`

## Запропоновані зміни

### `memory/domain/current/implementation-state.md`

1. Замінити exact рядок:

```md
- Production має internal readonly/full driver ports, Resource-only greedy index, deterministic semantic fake і internal concrete `local-sqlite-v1` durability adapter з Asset metadata, staged-generation persistence та bounded internal bytes lifecycle. Public final/default Storage Driver API, lazy mode, ordinary application-facing bytes/file API, multi-instance sync і release-wide support certification ще відсутні.
```

на:

```md
- Production має internal readonly/full driver ports, одну immutable coherent Resource/Asset/Mark generation з complete/selective coverage, deterministic semantic fake і internal concrete `local-sqlite-v1` durability adapter з Asset metadata, staged-generation persistence та bounded internal bytes lifecycle. Public final/default Storage Driver API, ordinary application-facing bytes/file API, concrete SQLite multi-instance sync/polling і release-wide support certification ще відсутні.
```

2. Додати exact блок безпосередньо перед unique anchor `## Поточні гарантії`:

```md
- `P5-WP1 / TASK-07.26-0058 / RUN-001` реалізувала одну immutable coherent read-model generation, complete/selective coverage, structural-sharing local publication, один process-local publication coordinator та narrow full/readonly semantic observation seams без другого Core, journal або map-swap path.
- `P5-HARD1 / TASK-07.26-0059 / RUN-001` реалізувала один admission-epoch single-flight synchronization actor із bounded retry/deadline/cancellation, coordinator-conflict retry, startup/stop/drain ownership та fail-close RuntimeFaultSink integration.
- `P5-VS1 / TASK-07.26-0060 / RUN-001` реалізувала `greedy`/`lazy` loading для чинних Resource point/tree reads та internal `asset.owner.get`/`mark.resources.get` selectors з exact generation-local positive/negative coverage, complete result metadata, pre-I/O revision capture, stale-observation re-observation і local-overlay preservation без partial success. Storage-global Mark observation є capability-gated: без exact selector capability вона повертає query unavailable до повного storage scan, а exact subset має selective observation stamp. Experimental descriptor-safe `readModel.loading`/`readModel.synchronization`, `query.refresh()` та frozen detached `inspect().read_model` використовують той самий actor/coordinator. Legacy manual `static-unsupported` не має cursor/head/actor; profile-specific/certified SQLite full+readonly observation, polling scheduler, two-process evidence і support claim лишаються P5-VS2/P5-STAB/P5-AUD1 gates.
```

3. Замінити exact substring:

```text
ordinary public bytes/file API, P5 indexes/sync, P7 compatibility freeze, final/public Storage Driver surface і power-loss certificate відсутні.
```

на:

```text
ordinary public bytes/file API, concrete SQLite multi-instance sync/polling, P7 compatibility freeze, final/public Storage Driver surface і power-loss certificate відсутні; P5 coherent generation, lazy complete-only Resource reads і semantic explicit refresh/config/inspection є current experimental implementation без topology support claim.
```

4. Замінити exact substring:

```text
P5 global indexes/sync, P7 compatibility freeze,
```

на:

```text
concrete P5 SQLite multi-instance sync/polling, P7 compatibility freeze,
```

5. Додати exact source line після existing `TASK-0053` source line:

```md
- `memory/tasks/plan/TASK-07.26-0060-p5-vs1-lazy-refresh-public-integration/RUN-001/result.md`.
```

### `memory/technical/architecture.md`

1. Замінити exact metadata line `Updated: 2026-07-17` на `Updated: 2026-08-23`.

2. Замінити exact paragraph під `## Статус реалізації` на:

```md
Цей документ описує цільову архітектуру зі draft-специфікацій. Фактично реалізовані internal pure domain contract kernel, IoC composition/conformance skeleton, strict internal lifecycle host/controller, одна coherent complete/selective read-model generation, shared Facade Provider/Registry, Operation Engine, deterministic full-driver fake/recovery, experimental public Resource/Asset lifecycle та bounded internal P4-VS3 upload bytes/finalization lifecycle. P5-WP1/P5-HARD1/P5-VS1 materialize-или один publication coordinator, один hardened synchronization actor/retry owner, `greedy`/`lazy` complete-only existing reads, descriptor-safe `readModel` config, explicit `query.refresh()` і safe frozen inspection через semantic full/readonly observation seams. Internal `embedded-transactional/local-sqlite-v1` durability driver лишається concrete Phase 4 profile; profile-specific/certified full+readonly observation, remaining-budget/lexical-seek behavior, production polling, two-process contention/visibility та topology support ще не реалізовані. Root package публікує `createExtensia`, opaque full-driver factory та bounded public contracts без internal Core/IoC/storage subpaths. Ordinary public upload/file API, public/default driver surface, P7 freeze і broader platform/performance/power-loss certification ще відсутні. Детальний current state зафіксовано в `memory/domain/current/implementation-state.md`.
```

3. Додати exact блок після existing paragraph `## Applied P5-DG2 target`:

```md

## P5-VS1 materialization

P5-VS1 bind-ить public query/config/inspection до того самого runtime-owned P5-HARD1 actor і P5-WP1 coordinator. Lazy observation виконує I/O поза mutation section, захоплює revision до I/O, publish-ить лише через compare-and-publish і re-observe-ить stale candidate. Selective local overlays зберігають local read-after-write, але identical або closure-subsumed tombstone authority не руйнує щойно доведений one-level coverage. Local journal sequence jump atomically залишає cursor позаду й робить safe freshness `unknown`; malformed observation/build contradiction fail-close-ить через existing RuntimeFaultSink. Supported readonly seam виконує лише startup/change observations і zero durable writes. Duplicate actor/coordinator/journal, hidden polling scheduler та accidental Core/storage export відсутні; concrete SQLite sync/polling лишається architecture stop boundary P5-VS2.

Storage-global Mark query не має implicit complete-read fallback: exact selector capability публікує selective subset з opaque stamp, а generic full/readonly seams без такої capability повертають query unavailable до acquisition/listing.
```

### `memory/technical/read-model-completeness-contract.md`

1. Замінити exact п'ять bullets під `### 2.2. Current facts` на:

```md
- Production index має одну immutable coherent generation з Resource/children, Asset owner/primary/lineage, exact Mark projections і generation-local coverage proofs.
- `greedy` full/readonly startup публікує complete generation; `lazy` після equivalent global integrity gate публікує empty/selective generation і догружає exact Resource point/one-level observations.
- Чинні public `getResource`/`getResourceTree` повертають success лише з exact coverage proof; negative proof зберігає missing semantics, unknown повертає typed storage read failure й не стає empty/partial success.
- Full/readonly semantic metadata adapters мають однакову claimed read semantics; readonly supported path виконує лише observation calls. Internal `asset.owner.get` повертає exact owner ID + Asset або proven absent з complete point metadata; `mark.resources.get` повертає exact sorted Resource IDs з complete storage-global metadata лише через exact selector capability, а без неї fail-ить query unavailable до implicit full scan. Public global query catalog не доданий.
- P5-WP1/P5-HARD1/P5-VS1 не реалізують profile-specific/certified `local-sqlite-v1` full+readonly observation, remaining-budget/lexical-seek behavior, polling scheduler або topology support; це лишається P5-VS2 і later gates.
```

2. Замінити exact paragraph під `## 14. Downstream contract` на:

```md
`P5-WP1`, `P5-HARD1` і `P5-VS1` materialized та accepted у TASK-0058/0059/0060; TASK-0060/FIX-001 applied. `P5-VS2` concrete multi-instance sync/polling/contention -> `P5-STAB` -> `P5-AUD1` лишаються prepared, inactive й потребують separate explicit activation. P5-VS1 acceptance/application не активує downstream packages.
```

3. Замінити exact paragraph під `## 15. Memory impact` на:

```md
P5-VS1 implementation потребує current/materialization sync через TASK-0060/FIX-001 у domain current state, architecture, P5 contracts, public-read compatibility note і roadmap. Product requirements, target domain invariants і normative completeness rules не змінюються.
```

### `memory/technical/multi-instance-synchronization-contract.md`

1. Замінити exact шість bullets під `### 2.3. Current-code facts` на:

```md
- P5-WP1/P5-HARD1/P5-VS1 реалізували один process-local coordinator з atomic generation/cursor/revision/known-behind state та один admission-epoch actor із bounded retry/deadline/cancellation/lifecycle ownership.
- Supported full semantic adapter capture-ить startup metadata+journal head і explicit committed-change refresh через той самий actor; supported readonly internal symbol capability має symmetric observation result і zero durable writes.
- Legacy manual `static-unsupported` branch не має cursor/head/actor й повертає exact public refresh-unavailable та safe `unsupported/startup` inspection.
- Local exact-next publication може advance-ити private cursor; sequence jump зберігає local delta, cursor позаду й safe freshness `unknown` без hidden manual work. Successful refresh повертає `observed-through`, а не permanent freshness.
- Experimental descriptor-safe `readModel.loading`/`readModel.synchronization`, `query.refresh(options?)` і frozen `inspect().read_model.synchronization` materialized без public cursor/head/runtime/storage leakage.
- Profile-specific/certified local SQLite full+readonly committed-change observation, lexical journal seek/remaining-budget behavior, production polling, two-process contention/visibility і support claim ще відсутні та належать P5-VS2/P5-STAB/P5-AUD1/human gate.
```

2. Замінити exact paragraph під `### 2.4. Support boundary` на:

```md
P5-VS1 реалізує semantic actor/coordinator/public integration, але не сертифікує live topology й не змінює current one-host/one-full-writer support boundary. Candidate topology стає supported лише після concrete P5-VS2 production implementation, two-process stabilization, independent phase audit і explicit human gate.
```

### `memory/technical/public-read-contract.md`

Додати exact блок після existing paragraph `## P5-DG2 compatibility note`:

```md

## P5-VS1 materialization note

`P5-VS1 / TASK-07.26-0060 / RUN-001` реалізувала optional descriptor-safe `readModel.loading`/`readModel.synchronization`, exact frozen `inspect().read_model` і `query.refresh(options?)` як `experimental-phase-5`. Existing `getResource`/`getResourceTree` names, value shapes, invalid/missing semantics і detached ownership не змінилися; lazy success потребує exact coverage proof. Default config лишається `greedy` + `manual` без background polling. Semantic full/readonly supported paths використовують один internal actor/coordinator; legacy driver повертає refresh unavailable. Packed root/API surface не експортує observation, cursor, actor, storage session або internal subpaths. Concrete SQLite sync/polling і P7 compatibility freeze не заявляються.
```

### `memory/product/roadmap.md`

1. Замінити exact metadata line `Updated: 2026-07-17` на `Updated: 2026-08-23`.

2. Замінити exact Phase 5 block від `Стан:` до final bullet перед `Gate:` на:

```md
Стан: P5-RS1 accepted; P5-DG1/P5-DG2 exact targets accepted/applied; P5-WP1, P5-HARD1 і P5-VS1 accepted; TASK-0060/FIX-001 applied. P5-VS2/P5-STAB/P5-AUD1 inactive.

Wave IDs: `P5-RS1` + `P5-DG1` + `P5-DG2` -> `P5-WP1` generation/coordinator foundation -> `P5-HARD1` internal retry/single-flight/lifecycle -> `P5-VS1` lazy + public explicit refresh/config -> pending `P5-VS2` concrete multi-instance sync/polling/contention -> `P5-STAB` -> `P5-AUD1` -> human gate.

- P5-WP1 реалізувала одну coherent generation для Resource/tree, Asset owner/primary/lineage і Mark projections та structural-sharing local publication.
- P5-VS1 реалізувала `greedy` complete і `lazy` selective internal coverage з complete-only existing Resource query success та exact internal Asset-owner/Mark-global selector results; Mark-global потребує exact selector capability й не запускає implicit full scan, а unknown дає explicit query-unavailable/read failure без partial success.
- P5-WP1/P5-VS1 реалізували narrow semantic full/readonly metadata + committed-change observation integration без raw session leakage; profile-specific/certified SQLite full+readonly observation і remaining-budget/lexical-seek behavior лишаються P5-VS2.
- P5-HARD1/P5-VS1 реалізували supported volatile cursor/coordinator actor semantics, legacy `static-unsupported`, experimental `query.refresh()`, bounded startup/refresh retry і safe inspection; production polling scheduler не реалізований.
- Initial same-host two-process `full/full`/`full/readonly` support лишається gated P5-VS2/P5-STAB/P5-AUD1/human gate; designated writer recommended.
```

3. Existing Phase 5 line `Gate:` лишається незмінною. Application не активує downstream task.

## Exactness verification перед application

- Перевірити, що кожний replacement/substring/append anchor вище має cardinality рівно `1`; при `0` або `>1` зупинитися без mutation.
- Застосувати всі шість target transformations атомарно в UTF-8; partial application заборонена.
- Перед mutation перевірити whole-task approval result і окремий explicit approval для `FIX-001`.
- Після application виконати Markdown/link/UTF-8/language/stale-wording checks, `git diff --check` і fresh independent post-application audit.

## Обґрунтування

Без fixation canonical current memory продовжує стверджувати, що production index має лише старі mutable Resource maps, lazy mode/public refresh відсутні, а generation revision/common coordinator не існують. Після P5-VS1 це суперечить коду й executable evidence. Proposal синхронізує лише фактичний semantic/public slice та навмисно не підміняє майбутню physical SQLite multi-instance верифікацію.

## Вплив і узгодженість

- `state.md` і task/run/progress lifecycle: operational updates included; recursive fixation не потрібна.
- Product roadmap: included; dependency chain не змінюється й P5-VS2 не активується.
- Domain current/target/rules: current included; target/rules не змінюються, бо canonical P5-DG1/P5-DG2 invariants уже applied.
- Technical architecture/contracts: current/materialization facts included; normative design не розширюється.
- Knowledge/project rules: not-needed.
- Wiki indexes: not-needed; structural canonical artifacts не додаються.
- Language gate: Ukrainian author text; stable identifiers retained.
- Architecture pressure: один actor, один coordinator, один local publication path і один runtime fault boundary збережені; concrete polling лишається P5-VS2 owner.

## Ризики

- Fixation не можна застосовувати без whole-task і separate `FIX-001` approval.
- Semantic fake/full/readonly conformance не можна описувати як concrete SQLite multi-process support.
- `polling` config validation/capability failure у P5-VS1 не є production polling scheduler.
- `observed-through` не є permanent freshness, а selective coverage не є storage-global completeness.
- Experimental Phase 5 surface не є final P7 compatibility freeze.

---

## Approval

Status: approved
Reviewed: 2026-08-23
Approval Source: direct user response `FIX-001: approve`
Approval Notes: whole-task окремо схвалено direct response `task: approve`; exact application authorized, downstream activation не надана.

## Застосування

Applied: yes — 2026-08-23
Applied Files: `memory/domain/current/implementation-state.md`; `memory/technical/architecture.md`; `memory/technical/read-model-completeness-contract.md`; `memory/technical/multi-instance-synchronization-contract.md`; `memory/technical/public-read-contract.md`; `memory/product/roadmap.md`
Verification: pre-application anchor cardinality `1`; exact atomic patch applied; UTF-8 strict decode, target existence, stale-wording scan, post-application exact-count checks і `git diff --check` PASS; fresh independent post-application audit `PASS`, open P0-P3 = 0
Deviations From Approved Proposal: none
Result: applied exactly and independently audited; no deviations, no open findings
