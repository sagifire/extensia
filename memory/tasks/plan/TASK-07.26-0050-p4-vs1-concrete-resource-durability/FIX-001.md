# Fixation: FIX-001

Status: applied
Requirement: required
Related Task: [P4-VS1 / TASK-07.26-0050](task.md)
Related Run: [RUN-001](RUN-001/index.md)
Related Research: none
Created: 2026-07-16
Review Status: passed
Review Record: [RUN-001 result](RUN-001/result.md)

## Призначення

Після whole-task approval синхронізувати canonical current/technical memory з фактичною accepted P4-VS1 Resource durability parity, не розширюючи internal composition до public/default driver API й не перебільшуючи bounded process-crash evidence.

## Джерело рішення

- Acceptance criteria й verified implementation `P4-VS1 / TASK-07.26-0050 / RUN-001`.
- Чинні accepted Phase 3 Resource semantics, P4-WP1 physical foundation і ADR-0010.

## Цільові файли

- `memory/domain/current/implementation-state.md`
- `memory/technical/architecture.md`
- `memory/technical/open-questions.md`

## Запропоновані зміни

### `memory/domain/current/implementation-state.md`

1. Змінити metadata:

```text
As Of: 2026-07-16
```

2. Замінити bullet:

```text
- `local-sqlite-v1` лишається internal і не додає root export або package subpath. `P4-VS1` Resource parity, Asset metadata/upload (`P4-VS2/P4-VS3`), public default-driver construction і ширші support/performance limits не прийняті цією задачею.
```

на:

```text
- `P4-VS1 / TASK-07.26-0050 / RUN-001` провела повний accepted Phase 3 Resource lifecycle через internal production composition `createLocalSqliteExtensia` → public facade → shared Core/Operation Engine → чинний `local-sqlite-v1`: create, own-metadata update, hierarchy move/dense order, Mark/KV replacement і leaf soft delete мають fake-vs-SQLite parity, durable read-back, contiguous committed journal, readonly byte invariance, restart/cut-point/process-crash і runtime corruption fail-close evidence.
- Internal production composition інкапсулює full/readonly concrete config і не є root export або package subpath. Packed probe підтверджує read-after-write/restart без path/schema/connection/session leakage; public/default driver construction, Asset metadata/upload (`P4-VS2/P4-VS3`), broader platform/power-loss certification і ширші performance limits лишаються неприйнятими.
```

3. У розділі `Поточні гарантії` замінити фінальні два речення абзацу:

```text
Internal concrete `local-sqlite-v1` physical durability реалізована й перевірена в bounded Windows local NTFS process-crash profile; final/public Storage Driver surface, power-loss certificate та downstream Resource/Asset phase acceptance відсутні. Цільові інваріанти ширших slices описані окремо в `memory/domain/target/model.md` і `memory/domain/rules.md`.
```

на:

```text
Internal concrete `local-sqlite-v1` physical durability і повна Phase 3 Resource parity через shared production pipeline реалізовані та перевірені bounded Windows local NTFS process-crash, fake-vs-SQLite, packed і fresh-process evidence. Final/public Storage Driver surface, power-loss certificate та downstream Asset phase acceptance відсутні. Цільові інваріанти ширших slices описані окремо в `memory/domain/target/model.md` і `memory/domain/rules.md`.
```

4. У розділі `Межа current/target` після речення про current implementation concrete adapter додати:

```text
Internal `createLocalSqliteExtensia` є production composition owner для full/readonly concrete Resource runtime, але не package API; через нього accepted create/update/move/Marks/KV/delete semantics доведені на реальному SQLite без другого Core, operation contract, index або journal path.
```

5. У `Джерела` додати:

```text
- `memory/tasks/plan/TASK-07.26-0050-p4-vs1-concrete-resource-durability/RUN-001/result.md`.
```

і змінити останню дату фактичної структури репозиторію на `2026-07-16`.

### `memory/technical/architecture.md`

1. Змінити metadata:

```text
Updated: 2026-07-16
```

2. У `Статус реалізації` замінити речення:

```text
Internal `embedded-transactional/local-sqlite-v1` concrete driver foundation реалізована через чинний opaque full-driver boundary і має bounded Windows local NTFS process-crash evidence; public/default driver surface, broader platform/power-loss certification і downstream Resource/Asset phase acceptance ще відсутні.
```

на:

```text
Internal `embedded-transactional/local-sqlite-v1` concrete driver і production composition реалізовані через чинний opaque full-driver boundary; повна Phase 3 Resource parity має bounded Windows local NTFS process-crash, fake-vs-SQLite, packed і fresh-process evidence. Public/default driver surface, broader platform/power-loss certification і downstream Asset phase acceptance ще відсутні.
```

3. Після абзацу `P4-WP1 materialize-ила...` додати:

```text
P4-VS1 materialize-ила один internal production composition owner `createLocalSqliteExtensia`, який вибирає full/readonly concrete adapter і передає його незміненому `createExtensia` composition path. Shared fake-vs-SQLite matrix доводить create/update/move/dense order/Marks/KV/leaf-delete parity, а packed і fresh-process probes — durable read-back, contiguous journal, readonly no-write, outcome-definite pre/post-COMMIT cut points і відсутність public physical-layout leakage. Root exports, package subpaths, Core contracts, Operation Engine, index publication і journal authority не розширені; другого write path або test-only architecture не створено.
```

### `memory/technical/open-questions.md`

Замінити bullet:

```text
- P4-WP1 реалізувала internal `local-sqlite-v1` і bounded Windows local fixed NTFS process-crash/lock/readonly/corruption/payload-seam evidence. Public/default driver surface, broader Node/OS/filesystem profiles, performance envelopes і destructive power-loss certificates лишаються окремими gates.
```

на:

```text
- P4-WP1 реалізувала internal `local-sqlite-v1`, а P4-VS1 довела повну Phase 3 Resource parity через shared production composition з bounded Windows local fixed NTFS process-crash/restart, readonly/corruption і packed evidence. Public/default driver surface, Asset persistence/upload, broader Node/OS/filesystem profiles, performance envelopes і destructive power-loss certificates лишаються окремими gates.
```

## Обгрунтування

Без fixation canonical memory продовжить стверджувати, що P4-VS1 Resource parity відсутня, хоча production source і executable evidence вже підготовлені до human review. Proposal відділяє internal accepted implementation від ще не прийнятих public/default, Asset, broad-platform, power-loss і performance claims.

## Вплив і узгодженість

- `state.md`: included як operational lifecycle update під час review/finalization; recursive fixation не потрібна.
- Product overview: not-needed - roadmap і dependency chain не змінені.
- Domain current/target/rules: included для current state; target/rules не змінюються, бо Resource semantics лишилися accepted Phase 3 contract.
- Technical architecture/rules/ADR: included для architecture/current open-question wording; rules та ADR-0010 не змінюються, бо physical/semantic protocol не змінено.
- Knowledge packages: not-needed - reusable methodology не змінена.
- Project-specific rules: not-needed.
- Wiki indexes: not-needed - structural canonical changes відсутні.
- Language gate: passed.
- Architecture pressure: checked - synchronous `DatabaseSync` лишається bounded profile cost; другого write path або public leakage не додано.

## Ризики

- Fixation не можна застосовувати до whole-task і separate FIX-001 approval.
- Wording має зберігати межу process-crash evidence й не перетворювати її на power-loss або universal-platform certificate.
- Internal packed artifact не повинен трактуватися як exported public/default driver surface.

---

## Approval

Status: approved
Reviewed: 2026-07-16
Approval Source: explicit user message: `FIX-001: approve`
Approval Notes: Exact reviewed proposal approved separately from whole-task result; application authorized without deviations.

## Застосування

Applied: 2026-07-16
Applied Files:

- `memory/domain/current/implementation-state.md` - current P4-VS1 implementation, guarantees, boundary, source і date synchronized exactly.
- `memory/technical/architecture.md` - implementation status, production composition paragraph і update date synchronized exactly.
- `memory/technical/open-questions.md` - P4 Resource parity gate closed while public/Asset/platform/performance/power-loss gates remain explicit.

Verification:

- Exact approved strings present; superseded P4-VS1 absent claims removed.
- Package export map remains exact root + `./package.json`; roadmap/target contracts/ADR/rules unchanged.
- `git diff --check` passed.

Deviations From Approved Proposal: none
Result: applied
