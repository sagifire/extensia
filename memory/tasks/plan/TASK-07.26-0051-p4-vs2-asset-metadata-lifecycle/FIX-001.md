# Fixation: FIX-001

Status: applied
Requirement: required
Related Task: [P4-VS2 / TASK-07.26-0051](task.md)
Related Run: [RUN-001](RUN-001/index.md)
Related Research: none
Created: 2026-07-16
Review Status: passed; independent post-application audit `PASS`
Review Record: [RUN-001 result](RUN-001/result.md)

## Призначення

Після whole-task і separate fixation approval синхронізувати canonical current/product/technical memory з фактичною accepted реалізацією P4-VS2 Asset metadata lifecycle, exact SQLite schema version `2` і bounded migration `1 → 2`, не заявляючи bytes transport, upload finish/abort, public concrete-driver API або P4-VS3 acceptance.

## Джерело рішення

- Frozen acceptance criteria й verified implementation `P4-VS2 / TASK-07.26-0051 / RUN-001`.
- Applied P4-DG2 [Asset Contract](../../../technical/asset-contract.md), accepted ADR-0010 та accepted P4-WP1/P4-VS1 foundation.

## Цільові файли

- `memory/domain/current/implementation-state.md`
- `memory/technical/architecture.md`
- `memory/technical/asset-contract.md`
- `memory/technical/decisions/ADR-0010-local-sqlite-storage-protocol.md`
- `memory/technical/open-questions.md`
- `memory/product/roadmap.md`

## Запропоновані зміни

### `memory/domain/current/implementation-state.md`

1. Замінити bullets про відсутню Asset persistence/permissive baseline і відсутню migration твердженнями:

```text
- `P4-VS2 / TASK-07.26-0051 / RUN-001` реалізувала experimental public Asset metadata create/update/set-primary/reassign/delete через той самий facade → shared Core/Operation Engine → driver transaction/journal → post-commit batch-index path. Exact descriptor-safe field/MIME/extension/WHATWG HTTP(S)/bounded JSON validation, global Asset identity, same-Resource ready-target lineage, primary/reassign/delete conflicts, common Asset/Resource timestamp і `RESOURCE_ASSET_UPLOAD_ACTIVE` мають executable evidence.
- External Asset одразу є ready URL metadata; initial internal Asset створюється лише як visible staged metadata + opaque active generation без bytes або ready publication. Ready/replacement states підтримують accepted metadata semantics, але begin/finish/abort і transport лишаються P4-VS3.
- Deterministic і concrete SQLite drivers commit-ять sorted Resource snapshots, exact `asset_changes`, generation/payload action і один journal row однією semantic transaction. Startup/coherent command load fail-close перевіряє ownership, global IDs, lifecycle/payload, timestamp, primary та lineage invariants; public snapshot/result/inspection не розкриває upload ID, path, session або SQL details.
- Generic public readonly startup fail-close відхиляє payload-dependent ambiguous uploading lineage target. Concrete SQLite readonly driver надає Core token-free readiness proof через internal symbol capability зі strict-validated payload state, без public DTO/method і без розкриття upload ID, path, session або SQL details; public `AssetSnapshot` shape лишається незмінною.
- `local-sqlite-v1` physical schema має current `user_version=2` і exact `asset_upload_generations`; full startup після strict validation атомарно мігрує accepted version `1` з порожнім legacy payload seam, а readonly version `1` валідовує без write. Unknown/corrupt/nonempty unsupported legacy state fail-close; це bounded versioned migration, не automatic repair.
```

2. У `Поточні гарантії` замінити фінальне речення про відсутню downstream Asset acceptance на bounded P4-VS2 current guarantee та explicit P4-VS3/P5/P7 exclusions.

3. У `Межа current/target` додати Asset metadata API, shared Asset write port, exact logical payload-action/journal seam і concrete staged-generation persistence як current; bytes transport, begin/finish/abort, payload publication/read, P5 indexes/sync, P7 freeze й public/default concrete driver лишити target/deferred.

4. У `Джерела` додати:

```text
- `memory/tasks/plan/TASK-07.26-0051-p4-vs2-asset-metadata-lifecycle/RUN-001/result.md`.
```

### `memory/technical/architecture.md`

1. Змінити metadata `Updated` на `2026-07-16`.

2. У `Статус реалізації` замінити downstream Asset absence на accepted P4-VS2 metadata slice через shared production pipeline, з explicit staged-only/P4-VS3 boundary.

3. Після `Applied P4-DG2 target` додати:

```text
P4-VS2 materialize-ила Asset metadata lifecycle без parallel engine або publication path: default facade передає normalized command у Core Asset port, який використовує той самий runtime-owned Operation Engine, identity source, Resource index і driver session/transaction. Effective transition готує canonical sorted Resource snapshots та exact sorted `asset_changes`, а driver atomically commit-ить metadata, generation/payload action і один journal row до batch-index publication. Coherent command loads і startup scan fail-close перевіряють storage-wide Asset/payload invariants. Generic public readonly boundary fail-close відхиляє ambiguous uploading lineage target, а concrete SQLite readonly driver надає Core token-free readiness proof через internal symbol capability зі strict-validated payload state. Initial internal create materialize-ить лише opaque staged generation; bytes, begin/finish/abort і ready publication не реалізовані до P4-VS3. Root runtime values та public `AssetSnapshot` shape не змінені.
```

4. У storage section зафіксувати current SQLite `user_version=2`, exact `asset_upload_generations` і strict atomic legacy `1 → 2` migration boundary без automatic repair.

### `memory/technical/asset-contract.md`

У `10. Compatibility review / Current AssetSnapshot` замінити obsolete future/current note на:

```text
Shape збережено. `P4-VS2 / TASK-07.26-0051 / RUN-001` materialize-ила це intentional tightening під `experimental-phase-4`: pure validators тепер перевіряють exact nonempty classifiers, canonical URL, bounded data й local aggregate shape, а coherent command/startup scan — global ownership/lifecycle/timestamp/primary/lineage invariants. Readiness proof concrete SQLite driver передає лише через internal symbol capability; public readonly DTO/method не додається. Bytes transport і upload finalization лишаються P4-VS3; P7 compatibility freeze не заявлений.
```

Downstream slicing і решту normative contract не переписувати: вони лишаються authority/історією owner gates, а materialization note відділяє current P4-VS2 від pending P4-VS3/P5/P7.

### `memory/technical/decisions/ADR-0010-local-sqlite-storage-protocol.md`

1. Додати metadata `Updated: 2026-07-16`.

2. Замінити version bullet:

```text
- `application_id`, current `user_version=2`, singleton format marker, exact schema/quick-check, canonical JSON/fingerprint/sequence/payload/generation integrity перевіряються до ready. Version `1` є єдиним accepted legacy input: full owner після повної validation й доказу порожнього legacy payload seam атомарно додає `asset_upload_generations` та оновлює обидва version markers; readonly валідовує version `1` без mutation. Unknown/corrupt або nonempty unsupported legacy state fail-close; deterministic schema migration не є automatic repair.
```

3. У `Наслідки` замінити obsolete “production capability ще не реалізована” на bounded P4-WP1/P4-VS1/P4-VS2 implementation fact та explicit uncertified platform/power-loss/public-driver boundary.

### `memory/technical/open-questions.md`

1. Змінити metadata `Updated` на `2026-07-16`.

2. У closed P4-DG1 baseline замінити “Asset persistence/upload” як один unresolved gate на: Asset metadata й staged-generation persistence accepted у P4-VS2; bytes transport/finalization, broader profiles, performance та destructive power-loss лишаються separate gates.

3. У closed P4-DG2 baseline замінити `Still open` на explicit remaining P4-VS3 bytes/begin/finish/abort/read adapter, performance/certification, P5 indexes/sync і P7 API/schema freeze.

### `memory/product/roadmap.md`

1. Змінити metadata `Updated` на `2026-07-16`.

2. Замінити Phase 4 status:

```text
Стан: P4-DG1/P4-DG2 approved/applied; P4-WP1 concrete driver, P4-VS1 Resource durability і P4-VS2 Asset metadata lifecycle accepted. P4-VS3 upload bytes/finalization та P4-STAB лишаються pending.
```

3. У Phase 4 bullets відмітити completed concrete driver/Resource parity/metadata lifecycle й лишити upload transport/finalization та повну crash/recovery stabilization як незавершені owners; gate wording не розширювати до power-loss або universal-platform claim.

## Обгрунтування

Без fixation canonical current memory продовжить стверджувати, що Asset writes, staged generation, concrete persistence і versioned migration відсутні, а ADR-0010 — що current schema має `user_version=1`. Proposal усуває ці суперечності, водночас відділяє реалізований metadata slice від P4-VS3 bytes/finalization та наступних index/sync/release gates.

## Вплив і узгодженість

- `state.md`: included як operational lifecycle update під час review/finalization; recursive fixation не потрібна.
- Product roadmap: included; Phase 4 factual progress змінюється, dependency chain не змінюється.
- Domain current/target/rules: current included; target/rules не змінюються, бо P4-DG2 contract уже applied.
- Technical architecture/rules/ADR: architecture, asset contract compatibility note й ADR-0010 included; technical rules уже містять applied P4-DG2 invariants і не потребують зміни.
- Knowledge packages: not-needed — reusable methodology не змінена.
- Project-specific rules: not-needed.
- Wiki indexes: not-needed — structural canonical artifacts не додаються.
- Language gate: passed.
- Architecture pressure: checked — один shared Operation Engine/driver transaction/journal/index path збережений; schema versioning лишається profile-local.

## Ризики

- Fixation не можна застосовувати без whole-task і separate `FIX-001` approval.
- `local-sqlite-v1` є profile name; `user_version=2` є current physical schema version, ці поняття не можна змішувати.
- Staged internal generation не можна описувати як ready payload, completed upload API або bytes durability proof.
- Process-crash/restart evidence не є destructive power-loss чи universal filesystem/platform certification.

---

## Approval

Status: approved
Reviewed: 2026-07-16
Approval Source: explicit user decision `FIX-001: approve`
Approval Notes: User separately approved whole TASK-0051 result and required FIX-001 application.

## Застосування

Applied: 2026-07-16
Applied Files: `memory/domain/current/implementation-state.md`; `memory/technical/architecture.md`; `memory/technical/asset-contract.md`; `memory/technical/decisions/ADR-0010-local-sqlite-storage-protocol.md`; `memory/technical/open-questions.md`; `memory/product/roadmap.md`
Verification: self-review, UTF-8 replacement-character scan, `git diff --check` і repeated independent post-application audit `PASS`; open P0-P3: none.
Deviations From Approved Proposal: none in semantic scope. Post-application audit required minimal stale Phase 3 current-status synchronization inside the already approved roadmap/architecture targets; it only removed contradictions and was independently re-audited.
Result: canonical current/product/technical memory synchronized with accepted P4-VS2; P4-VS3/P5/P7, destructive power-loss і universal-platform claims remain deferred.
