# FIX-001: Зафіксувати реалізований internal local SQLite driver foundation

Status: applied
Requirement: required
Related Task: [P4-WP1 / TASK-07.26-0049](task.md)
Related Run: [RUN-001](RUN-001/index.md)
Prepared: 2026-07-15
Approval: approved by explicit user decision 2026-07-15
Applied: yes, 2026-07-15

## Мета

Після whole-task approval синхронізувати canonical current/technical memory з фактичною реалізацією `P4-WP1`, не перебільшуючи public surface, Asset readiness, power-loss evidence або downstream `P4-VS1` acceptance.

## Target files і exact proposed changes

### 1. `memory/domain/current/implementation-state.md`

1. Змінити metadata:

```text
As Of: 2026-07-15
```

2. Після bullet про `TASK-07.26-0036 / RUN-002` додати:

```text
- `P4-WP1 / TASK-07.26-0049 / RUN-001` реалізувала internal `embedded-transactional/local-sqlite-v1` через built-in Node.js 24 `node:sqlite`: driver-owned `extensia.sqlite3`, exact format/schema/pragma gate, canonical Resource/journal encoding, one-transaction Resource+operation+journal commit, exclusive connection lease, outcome-definite COMMIT reconciliation, recovery-before-ready, readonly no-write та fail-close integrity validation через чинний `FullResourceDriverAdapter`.
- Physical evidence на Node.js `v24.17.0`, SQLite `3.53.0`, Windows local fixed NTFS довела cross-process lock retention після COMMIT до session release, crash release/rollback, reopen recovery, 64 KiB chunked 1 MiB payload-seam rollback і unsupported/corrupt/readonly negative paths. Supported profile вимагає caller-supplied trusted attestation, звірену з canonical root; library не заявляє built-in auto-detection NTFS/network/sync truth. Це process-crash certificate поточного exact host/root, а не загальна power-loss, network/removable/sync/FUSE, multi-host або arbitrary Node/OS/filesystem гарантія.
- `local-sqlite-v1` лишається internal і не додає root export або package subpath. `P4-VS1` Resource parity, Asset metadata/upload (`P4-VS2/P4-VS3`), public default-driver construction і ширші support/performance limits не прийняті цією задачею.
```

3. Замінити bullet:

```text
- Production має мінімальний internal readonly driver port і Resource-only greedy index, але не final/public Storage Driver API, concrete durable driver, lazy mode, persistence або recovery semantics.
```

на:

```text
- Production має internal readonly/full driver ports, Resource-only greedy index, deterministic semantic fake і internal concrete `local-sqlite-v1` durability adapter. Public final/default Storage Driver API, lazy mode, Asset persistence/upload, multi-instance sync і release-wide support certification ще відсутні.
```

4. Замінити bullets:

```text
- Public opaque full-driver handle є experimental; deterministic fake доводить protocol/recovery behavior, але не concrete physical durability. Restore/include-deleted/cascade/purge/retention, plugins, sync і concrete driver ще відсутні.
- P4-DG1 accepted target design визначає families `filesystem-native`, `embedded-transactional`, `client-server-transactional` і first/default profile `local-sqlite-v1`. Це design state: production SQLite/filesystem/PostgreSQL/MySQL driver не реалізований і не сертифікований.
```

на:

```text
- Public opaque full-driver handle є experimental; deterministic fake лишається semantic oracle, а internal `local-sqlite-v1` додає bounded concrete process/crash durability evidence. Restore/include-deleted/cascade/purge/retention, plugins, sync, Asset persistence/upload і public default-driver surface ще відсутні.
- P4-DG1 accepted target design визначає families `filesystem-native`, `embedded-transactional`, `client-server-transactional` і first/default profile `local-sqlite-v1`. P4-WP1 реалізувала internal SQLite profile і exact current-environment process-crash proof; filesystem-native/PostgreSQL/MySQL drivers не реалізовані, а ширша platform/power-loss certification не заявлена.
```

5. У `Поточні гарантії` замінити фінальне речення `Concrete physical durability і final Storage Driver відсутні.` на:

```text
Internal concrete `local-sqlite-v1` physical durability реалізована й перевірена в bounded Windows local NTFS process-crash profile; final/public Storage Driver surface, power-loss certificate та downstream Resource/Asset phase acceptance відсутні.
```

6. У `Межа current/target` після речення про Mark/KV додати:

```text
Internal `local-sqlite-v1` adapter, exact schema/format gate, one-transaction Resource+journal commit, exclusive lease, COMMIT reconciliation, readonly/recovery/integrity behavior і bounded process-crash proof також є current implementation.
```

і в останньому переліку замінити `final Storage Driver semantics, concrete physical durability` на `final/public Storage Driver surface, broader physical certification`.

7. У `Джерела` додати:

```text
- `memory/tasks/plan/TASK-07.26-0049-p4-wp1-local-sqlite-driver/RUN-001/result.md`.
```

### 2. `memory/technical/architecture.md`

1. Змінити metadata:

```text
Updated: 2026-07-15
```

2. У `Статус реалізації` замінити речення:

```text
Final concrete Storage Driver і physical durability ще відсутні.
```

на:

```text
Internal `embedded-transactional/local-sqlite-v1` concrete driver foundation реалізована через чинний opaque full-driver boundary і має bounded Windows local NTFS process-crash evidence; public/default driver surface, broader platform/power-loss certification і downstream Resource/Asset phase acceptance ще відсутні.
```

3. Наприкінці `Architecture health` додати:

```text
P4-WP1 materialize-ила `local-sqlite-v1` як один internal adapter за чинним `FullResourceDriverAdapter`: SQLite schema/path/pragma/lock/recovery лишаються profile-local, Resource canonical JSON використовує shared storage codec, а Core/Operation Engine/public root не отримали SQL або physical layout knowledge. Один SQLite transaction є authority для Resource rows, operation idempotency і committed journal; external blob, independent journal, PID lock, ORM/dependency або parallel write path не додані. Exact schema/integrity/readonly gates, COMMIT reconciliation, cross-process contention і crash rollback мають executable evidence; ширші support claims лишаються окремими gates. Нового істотного architecture pressure або workaround не виявлено.
```

### 3. `memory/technical/open-questions.md`

1. Змінити metadata:

```text
Updated: 2026-07-15
```

2. У `Runtime і storage` замінити питання:

```text
- Які tested environment/performance limits сертифікують `local-sqlite-v1` після P4-WP1 crash/lock/payload evidence?
```

на:

```text
- Які додаткові Node.js minor, Windows/NTFS device/cache, Linux ext4/XFS, payload-volume/latency та destructive power-loss profiles треба сертифікувати понад bounded P4-WP1 Windows local NTFS process-crash evidence?
```

3. У `Закритий P4-DG1 baseline` замінити:

```text
- Physical implementation/certification ще не виконані; P4-WP1 лишається окремим gate.
```

на:

```text
- P4-WP1 реалізувала internal `local-sqlite-v1` і bounded Windows local fixed NTFS process-crash/lock/readonly/corruption/payload-seam evidence. Public/default driver surface, broader Node/OS/filesystem profiles, performance envelopes і destructive power-loss certificates лишаються окремими gates.
```

## Rationale

Без fixation canonical memory продовжить стверджувати, що concrete SQLite driver відсутній, хоча production source, package artifacts і executable evidence вже існують. Proposal навмисно розділяє current internal implementation від ще не прийнятих public/default, Asset, P4-VS1, multi-instance, broad-platform і power-loss claims.

## Impact і risks

- Product: first concrete internal durability foundation стає current після approval/application; release completion не заявляється.
- Domain current: відображає фактичний source/test boundary без змішування з target Asset/sync state.
- Technical: current architecture та open questions синхронізуються з evidence.
- Public API/package: root exports і dependencies незмінні; internal compiled module не стає exported subpath.
- Risk: wording може бути прочитане як universal SQLite certification; exact bounded qualifiers вище є обов’язковими й не скорочуються під час application.

## Upward consistency

- `memory/state.md`: included як operational lifecycle update під час finalization; recursive fixation не потрібна.
- Product roadmap: not-needed; target sequence вже містить P4-WP1 → P4-VS1.
- Domain current: included.
- Domain target/rules: not-needed; semantics не змінені.
- Technical architecture: included.
- Technical rules/ADR-0010/write-recovery contract: not-needed; implementation відповідає accepted contract.
- Technical open questions: included.
- Knowledge packages: not-needed.
- Wiki indexes: not-needed; structural links не змінюються.

## Verification before application

- Whole-task P4-WP1 approval і окреме approval required FIX-001.
- Independent audit `REVIEW_READY` без open P0-P3.
- Exact proposal diff review; жодного universal/public/power-loss claim.
- Після application: повторний consistency scan і append-only finalization evidence в RUN-001 result.

## Language gate

Пройдено: canonical proposed text український; API/SQL/profile/status identifiers залишені у сталій технічній формі.
