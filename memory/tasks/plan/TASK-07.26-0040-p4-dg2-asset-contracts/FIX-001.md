# FIX-001: Зафіксувати exact Asset semantic contract

Status: applied
Requirement: required
Related Task: [P4-DG2 / TASK-07.26-0040](task.md)
Related Run: [RUN-001](RUN-001/index.md)
Source Research: [RSCH-001](RSCH-001.md)

## Мета

Після окремого fixation-specific human approval застосувати exact canonical Asset contract без нового author judgment. Proposal не застосовано.

## Normative application payload

Цей розділ є єдиним нормативним application payload. Application має виконати всі операції нижче exact; partial application заборонена. Якщо source hash/anchor не збігається, application зупиняється як conflict і не адаптує текст вручну.

### Source precondition manifest

Перед будь-якою mutation перевірити всі SHA-256 UTF-8 bytes. Будь-який mismatch зупиняє application до нового reviewed run/proposal.

| Existing target | Required SHA-256 |
|---|---|
| `memory/domain/rules.md` | `8fcebcf9089c999ee4a20b5d725b97d6469fb26377217ca89fc6220b4ffc2817` |
| `memory/domain/target/model.md` | `9fb2a50d46fbd5b0c7afe6589dac0ac5fd05679c07247d662f78ec931dddb277` |
| `memory/domain/open-questions.md` | `e59dbd37cf41916d98e721b7e633d3dbbf2f58936b02a53ec2b1bd268d06241b` |
| `memory/domain/current/implementation-state.md` | `0f869e3e00c606bcba077dcfa0ca813567e61f12ba81831d6604cb79d900e58d` |
| `memory/technical/architecture.md` | `01c45e20dba4cfb518369c9549a87f6026c41f47da9a3593627260e7c865ebba` |
| `memory/technical/rules.md` | `a73c2c6312e33c262b3937fab49d32bdbf8427857f0cc97902bced860cb7d7a3` |
| `memory/technical/public-read-contract.md` | `793db45ef84cd3a91908fb42da928252c4a40dcaeab1c95e91a15a080224e26c` |
| `memory/technical/write-journal-recovery-contract.md` | `5af0ef18d7842829d02f9955920dd7382af7c4e170eeae86cdf8831965e0c242` |
| `memory/technical/order-delete-mark-kv-contract.md` | `147f85cb1e8566d90d5130fc0ef96cd685ac6faccc53c1016ba2443f01e1152b` |
| `memory/technical/open-questions.md` | `84c5fb939bbefb7a19bcfb49afbbeae91757b8a2998ce1c877a5f39bc7cb1b35` |
| `memory/technical/index.md` | `164701043722be269490692aa60cc84401c2ef9eb5a0af764ca94def1b79ecd6` |
| `memory/technical/decisions/index.md` | `2cbaa58adbdc538bc02991ce6e4eeb3aa083df992a7d30166cb97004c227f47e` |
| `memory/product/roadmap.md` | `672945dd57338cfb3b69129d5f9aa8e8d2db525c3b182c5644a9a9ecf969298e` |
| `memory/state.md` | `8588828e4276d68b2309f184bec8883f67cea6c36de7cfe45efa94fe60ba91b8` |

Також вимагати absence майбутніх targets `memory/technical/asset-contract.md` і `memory/technical/decisions/ADR-0011-asset-semantic-lifecycle.md`.

### A. `memory/technical/asset-contract.md` — deterministic create

1. Перевірити SHA-256 UTF-8 bytes файла `memory/reports/research/2026-07-15-extensia-asset-contracts.md`:

```text
d9d0d94ae8b58c35ba5257153f3f3510f81607f2b490726e2b38c79d545247c1
```

2. Скопіювати весь файл byte-for-byte у `memory/technical/asset-contract.md`.
3. Замінити exact початковий block:

```md
# Exact Asset semantic contract Extensia

Status: proposed for human review
Related Task: [P4-DG2 / TASK-07.26-0040](../../tasks/plan/TASK-07.26-0040-p4-dg2-asset-contracts/task.md)
Related Run: [RUN-001](../../tasks/plan/TASK-07.26-0040-p4-dg2-asset-contracts/RUN-001/index.md)
Task-local Research: [RSCH-001](../../tasks/plan/TASK-07.26-0040-p4-dg2-asset-contracts/RSCH-001.md)
Date: 2026-07-15
```

на exact block:

```md
# Asset semantic contract

Status: accepted target contract
Accepted: 2026-07-15
Authority: P4-DG2 / TASK-07.26-0040 / approved FIX-001
Compatibility: `experimental-phase-4`
Detailed Design: [Exact Asset semantic contract](../reports/research/2026-07-15-extensia-asset-contracts.md)
```

Інших змін у copied body не робити.

4. Вимагати exact SHA-256 created UTF-8 bytes:

```text
b18622dacba7c68f92c8fcbb87881d81a212b331042afa6594f1247c55002f05
```

### B. `memory/technical/decisions/ADR-0011-asset-semantic-lifecycle.md` — exact create

Створити exact content:

```md
# ADR-0011: Asset semantic lifecycle

Status: accepted target design
Date: 2026-07-15
Decision Owner: P4-DG2 / TASK-07.26-0040
Evidence: `memory/reports/research/2026-07-15-extensia-asset-contracts.md`

## Контекст

Phase 4 потребує exact Asset metadata/file-state semantics поверх accepted one-commit Core/Operation Engine і P4-DG1 durability boundary без стабілізації physical layout або bytes transport.

## Рішення

- `derived_from` є hard same-Resource relation до existing ready representation; self-link, cross-Resource, dangling і cycles заборонені. Delete target with dependents та reassign linked Asset fail conflict без implicit rewrite.
- Primary selection/clear explicit. Delete primary leaves none; initial-uploading internal Asset cannot be primary; reassign переносить Asset non-primary; auto-promotion відсутня.
- Internal create staged-only. Internal durable state розрізняє initial-uploading, ready і replacement-uploading; replacement зберігає old committed payload visible до finish. Atomic ready-file create відхилено для `0.1.0`.
- Resource delete з будь-яким active Asset upload повертає `RESOURCE_ASSET_UPLOAD_ACTIVE` до mutation; caller finish/abort/delete Asset first. Resource tombstone не може strand-ити active generation.
- Asset metadata, owning Resource timestamps, exact compound payload action і one journal entry commit-яться однією Core/Operation Engine semantic operation; staged bytes never visible; separate file/journal publication path forbidden.
- Public `AssetSnapshot` shape unchanged. Upload generation/handle є internal, не public DTO/path/session. P4-VS3 owns executable Core↔driver staging/transport adapter boundary.

## Alternatives

- Cross-Resource lineage — відхилено через global referential locks/cascade/index blast radius.
- Implicit primary promotion/demotion — відхилено через hidden aggregate mutations.
- Atomic ready-file create — відхилено як premature transport/publication path.
- Hide old payload during replacement — відхилено; last committed payload remains readable.
- Resource-delete upload cascade — відхилено; explicit conflict preserves P3 no-cascade behavior.

## Наслідки

P4-VS2 може реалізувати exact metadata operations і validator/startup integrity. P4-VS3 додає opaque generation begin/finish/abort/retry на driver capability. Physical SQL/layout/chunks, application bytes transport, P5 indexes і P7 compatibility лишаються окремими owners.
```

### C. Exact append blocks

Append each block once at EOF of target file, except `domain/current/implementation-state.md`, whose block uses the explicit anchor below.

#### `memory/domain/rules.md`

```md

## Applied P4-DG2 Asset refinement

1. Asset ID globally unique in storage; кожен Asset належить одному active owning Resource для writes, а aggregate зберігає Assets sorted binary by ID.
2. Exact field/URL/data envelope визначає `technical/asset-contract.md`; silent normalization відсутня, крім canonical UUID/WHATWG URL.
3. `derived_from` посилається лише на existing ready Asset того самого Resource, не self і не створює cycle/dangling relation.
4. Primary selection/clear explicit; initial-uploading internal Asset не primary; delete primary leaves none; reassign clears primary й дозволений лише без lineage/active upload.
5. Internal create staged-only; replacement тримає last committed payload visible. Resource delete за active upload fail-ить `RESOURCE_ASSET_UPLOAD_ACTIVE` до mutation.
6. Effective Asset/file-state transition bump-ить changed Asset та owning Resource common Timestamp; no-change/failure не змінює persisted timestamps.
```

#### `memory/domain/target/model.md`

```md

## Applied P4-DG2 Asset lifecycle

Canonical exact field, relation, primary, ownership і lifecycle semantics визначає [Asset semantic contract](../../technical/asset-contract.md). Same-Resource ready-target lineage є hard invariant. Internal lifecycle: initial-uploading → ready та ready → replacement-uploading → ready; abort initial removes Asset, abort replacement restores old ready state. Upload generation state internal і не розширює `AssetSnapshot`. Physical driver mechanics не є domain model.
```

#### `memory/technical/architecture.md`

```md

## Applied P4-DG2 target

Asset operations розширюють той самий facade → Core port → Operation Engine → driver semantic commit pipeline. Full sorted Resource snapshots, exact logical Asset change, compound payload action і one journal entry формують один prepared write-set; reassign atomically stages two Resources. Driver generation state не є public DTO/path. Index batch publish-иться post-commit; P5 indexes derived. Separate staged-file publication, cross-Resource lineage cascade або facade/direct-driver upload write є architecture stop condition.
```

#### `memory/technical/rules.md`

```md

## Applied P4-DG2 rules

- Asset fields/URL/data use exact descriptor-safe bounded contract; only UUID and WHATWG HTTP(S) URL canonicalize.
- Lineage same-Resource, ready-target, no self/cycle/dangling; primary explicit; reassign lineage-free/non-primary/no-active-upload.
- Initial internal lifecycle staged-only; replacement exposes only last committed payload; Resource delete conflicts on active upload.
- Every effective transition uses common Asset/Resource Timestamp, exact frozen locks/prepared Resources/payload action, one semantic commit/journal and post-commit atomic index swap.
- Readonly precedes input/staging; no-change/failure has no persisted timestamp/journal mutation; proven Asset integrity fail-close maps to `STORAGE_INTEGRITY_FAILED`.
- Physical layout, bytes transport, P5 indexes і P7 compatibility remain separate owners.
```

#### `memory/technical/public-read-contract.md`

```md

## P4-DG2 compatibility note

Public `AssetSnapshot` field shape is unchanged. Accepted target valid values/aggregate relations are tightened by [Asset semantic contract](asset-contract.md). P4 metadata methods/errors remain `experimental-phase-4` until implementation and P7 compatibility freeze; Phase 2 implemented reads are not rewritten by this note.
```

#### `memory/technical/write-journal-recovery-contract.md`

```md

## P4-DG2 Asset refinement

Asset operations reuse unchanged outcome-definite semantic commit. Prepared fingerprint binds sorted full Resource snapshots, exact logical Asset change and discriminated compound payload action/upload generation without path leakage. Metadata, payload action and exactly one journal row commit atomically; independent file publication remains forbidden. This does not retroactively widen bounded P3 public Resource commands.
```

#### `memory/technical/order-delete-mark-kv-contract.md`

```md

## P4-DG2 Resource delete refinement

Once Assets exist, `deleteResource` checks latest coherent target after existing active-children conflict. Any `is_on_uploading=true` Asset returns normalized `RESOURCE_ASSET_UPLOAD_ACTIVE` before transaction/timestamp/journal mutation. Caller must finish/abort/delete active uploads first; delete does not cascade upload cleanup. This code extends `ResourceDeleteError` in Phase 4.
```

#### `memory/technical/open-questions.md`

```md

## Закритий P4-DG2 baseline

- U-21: lineage same-Resource/ready/acyclic/no-dangling; primary explicit, delete leaves none, reassign non-primary and lineage-free.
- U-22: exact field/MIME/extension/WHATWG HTTP(S) URL validation fixed in `asset-contract.md`.
- U-23: exact `Asset.data` depth/node/container/string/canonical UTF-8 limits fixed; schema evolution remains P7-WP1.
- U-24: internal create staged-only; replacement retains old committed payload; active upload blocks Resource delete.
- Still open: application bytes transport, executable driver upload adapter/performance/certification, P5 global indexes/sync and P7 API/schema freeze.
```

#### `memory/domain/current/implementation-state.md`

Insert immediately after exact existing bullet:

```md
- P4-DG1 accepted target design визначає families `filesystem-native`, `embedded-transactional`, `client-server-transactional` і first/default profile `local-sqlite-v1`. Це design state: production SQLite/filesystem/PostgreSQL/MySQL driver не реалізований і не сертифікований.
```

```md

- P4-DG2 accepted target design defines exact Asset fields/lineage/primary/reassign/staged lifecycle, але current code still has only permissive baseline snapshot validation: Asset writes, upload generation/runtime, `RESOURCE_ASSET_UPLOAD_ACTIVE` і concrete durability не реалізовані.
```

### D. Exact replacement: `memory/domain/open-questions.md`

Replace exact block:

```md
## Asset

- Чи буде `derived_from` між assets різних Resources дозволений у stabilized contract, чи бажане same-resource правило стане hard invariant?
- Що відбувається з primary designation під час delete/move primary Asset?
- Які validation/normalization rules потрібні для `type`, `role`, `mime`, `extension` і external `url`?
- Які size limits, schema-versioning policy та додаткові compatibility constraints потрібні для `Asset.data` поверх уже прийнятого readonly JSON-safe object-or-null baseline?
- Чи може internal Asset створюватися одразу з готовим file без visible `is_on_uploading = true` phase?
```

with:

```md
## Asset

Немає відкритих P4-DG2 blockers. Application bytes transport, P5 indexes/sync і P7 schema/API freeze лишаються technical owner gates.

## Закритий P4-DG2 baseline

- `derived_from` same-Resource, ready-target, acyclic і no-dangling; delete/reassign conflicts без implicit rewrite.
- Primary explicit; delete leaves none; reassign non-primary; initial-uploading not primary.
- Exact fields/MIME/extension/WHATWG HTTP(S) URL та `Asset.data` limits defined in `technical/asset-contract.md`.
- Internal create staged-only; replacement retains last committed payload; active upload blocks Resource delete.
```

### E. Exact index appends

Append under `## Файли` once.

`memory/technical/index.md`:

```md
- [Asset Semantic Contract](asset-contract.md) - Accepted P4-DG2 fields, lineage, primary, ownership, staged lifecycle і one-commit boundary.
- [ADR-0011: Asset semantic lifecycle](decisions/ADR-0011-asset-semantic-lifecycle.md) - Same-Resource lineage, explicit primary, staged-only initial state і Resource-delete upload conflict.
```

`memory/technical/decisions/index.md`:

```md
- [ADR-0011: Asset semantic lifecycle](ADR-0011-asset-semantic-lifecycle.md) - Same-Resource lineage, explicit primary, staged-only internal lifecycle і one-commit payload action.
```

### F. Exact lifecycle replacements

`memory/product/roadmap.md`: replace

```md
Стан: P4-DG1 approved/applied design; concrete implementation ще не створена/активована.
```

with

```md
Стан: P4-DG1 і P4-DG2 approved/applied target designs; concrete driver/Asset implementation ще не створена/активована.
```

`memory/state.md`: replace current-focus paragraph

```md
Phase 1, Phase 2 і Phase 3 завершені. `P4-DG1 / TASK-0039` accepted: approved FIX-001/FIX-002 applied як ADR-0010, concrete profile і taxonomy; production driver ще не реалізований. P4-DG2, TASK-0041 і TASK-0042 лишаються backlog/prepared.
```

with

```md
Phase 1, Phase 2 і Phase 3 завершені. `P4-DG1 / TASK-0039` і `P4-DG2 / TASK-0040` accepted/applied target designs; production driver та Asset writes/upload ще не реалізовані. TASK-0041 і TASK-0042 лишаються backlog/prepared.
```

In `memory/state.md`, replace active-task line

```md
- `P4-DG2 / TASK-07.26-0040` backlog/prepared: exact Asset contracts design/research, не activated.
```

with

```md
- `P4-DG2 / TASK-07.26-0040` review/finalizing: whole-task і FIX-001 approved; exact canonical application виконується, downstream не активовано.
```

In `memory/state.md`, replace next-step line

```md
1. Окремим рішенням активувати P4-DG2, TASK-0041 або TASK-0042, або створити owner preparation P4-WP1 поверх published APP-07.26-0039-001; жоден downstream task автоматично не активований.
```

with

```md
1. Окремим owner рішенням підготувати/активувати наступний Phase 4 slice за prerequisites або активувати TASK-0041/TASK-0042; application P4-DG2 не активує downstream автоматично.
```

### G. Post-application verification

- Recompute hash of created `asset-contract.md` and record it in application evidence.
- Validate every local Markdown link, wiki index і UTF-8 language gate.
- Search canonical memory for old cross-Resource weak-lineage/atomic-ready/open-P4-DG2 claims; only historical source/report/task artifacts may retain them.
- Confirm no code/dependency/public export changed and no downstream task activated.
- Run independent post-application audit before task completion.

### H. Exact compile-contract replacements: `memory/technical/order-delete-mark-kv-contract.md`

Replace exact union:

```ts
type ExtensiaErrorCode =
  | 'CONFIG_INVALID' | 'MODULE_BUSY' | 'MODULE_INVALID_STATE'
  | 'MODULE_NOT_READY' | 'START_FAILED' | 'STOP_FAILED'
  | 'INVALID_RESOURCE_ID' | 'RESOURCE_NOT_FOUND' | 'STORAGE_READONLY'
  | 'RESOURCE_INPUT_INVALID' | 'RESOURCE_NO_CHANGES'
  | 'RESOURCE_ID_GENERATION_FAILED' | 'STORAGE_LOCK_FAILED'
  | 'STORAGE_WRITE_FAILED' | 'STORAGE_INTEGRITY_FAILED'
  | 'RESOURCE_PARENT_NOT_FOUND' | 'RESOURCE_MOVE_CYCLE'
  | 'RESOURCE_ORDER_OUT_OF_RANGE' | 'RESOURCE_HAS_CHILDREN'
  | 'RESOURCE_ALREADY_DELETED'
```

with:

```ts
type ExtensiaErrorCode =
  | 'CONFIG_INVALID' | 'MODULE_BUSY' | 'MODULE_INVALID_STATE'
  | 'MODULE_NOT_READY' | 'START_FAILED' | 'STOP_FAILED'
  | 'INVALID_RESOURCE_ID' | 'RESOURCE_NOT_FOUND' | 'STORAGE_READONLY'
  | 'RESOURCE_INPUT_INVALID' | 'RESOURCE_NO_CHANGES'
  | 'RESOURCE_ID_GENERATION_FAILED' | 'STORAGE_LOCK_FAILED'
  | 'STORAGE_WRITE_FAILED' | 'STORAGE_INTEGRITY_FAILED'
  | 'RESOURCE_PARENT_NOT_FOUND' | 'RESOURCE_MOVE_CYCLE'
  | 'RESOURCE_ORDER_OUT_OF_RANGE' | 'RESOURCE_HAS_CHILDREN'
  | 'RESOURCE_ALREADY_DELETED' | 'RESOURCE_ASSET_UPLOAD_ACTIVE'
  | 'INVALID_ASSET_ID' | 'ASSET_INPUT_INVALID' | 'ASSET_URL_INVALID'
  | 'ASSET_DATA_INVALID' | 'ASSET_NOT_FOUND' | 'ASSET_NO_CHANGES'
  | 'ASSET_ID_GENERATION_FAILED' | 'ASSET_PRIMARY_CONFLICT'
  | 'ASSET_NOT_READY' | 'ASSET_LINEAGE_INVALID'
  | 'ASSET_LINEAGE_CONFLICT' | 'ASSET_HAS_DERIVATIVES'
  | 'ASSET_UPLOAD_ALREADY_ACTIVE' | 'ASSET_UPLOAD_NOT_ACTIVE'
  | 'ASSET_UPLOAD_INCOMPLETE' | 'ASSET_FILE_NOT_READY'
```

Replace exact alias:

```ts
type ResourceDeleteError =
  | ErrorOf<'MODULE_NOT_READY'> | ErrorOf<'STORAGE_READONLY'>
  | ErrorOf<'INVALID_RESOURCE_ID'> | ErrorOf<'RESOURCE_NOT_FOUND'>
  | ErrorOf<'RESOURCE_HAS_CHILDREN'> | ErrorOf<'RESOURCE_ALREADY_DELETED'>
  | ErrorOf<'STORAGE_LOCK_FAILED'> | ErrorOf<'STORAGE_WRITE_FAILED'>
  | ErrorOf<'STORAGE_INTEGRITY_FAILED'>
```

with:

```ts
type ResourceDeleteError =
  | ErrorOf<'MODULE_NOT_READY'> | ErrorOf<'STORAGE_READONLY'>
  | ErrorOf<'INVALID_RESOURCE_ID'> | ErrorOf<'RESOURCE_NOT_FOUND'>
  | ErrorOf<'RESOURCE_HAS_CHILDREN'> | ErrorOf<'RESOURCE_ASSET_UPLOAD_ACTIVE'>
  | ErrorOf<'RESOURCE_ALREADY_DELETED'> | ErrorOf<'STORAGE_LOCK_FAILED'>
  | ErrorOf<'STORAGE_WRITE_FAILED'> | ErrorOf<'STORAGE_INTEGRITY_FAILED'>
```

Replace exact precedence row:

```md
| 9 | missing/deleted parent, cycle, then range | active children | resulting aggregate validation | resulting namespace/resource count/total validation |
```

with:

```md
| 9 | missing/deleted parent, cycle, then range | active children, then active Asset upload | resulting aggregate validation | resulting namespace/resource count/total validation |
```

## Impact and risks

- Product: design-unblocks P4-VS2/P4-VS3 after owner preparation; no activation.
- Domain/API: tightens target values/relations without changing `AssetSnapshot` fields.
- Technical: reuses one Core/Operation Engine commit and adds internal generation state/Resource-delete conflict.
- Risks: current validator requires future deliberate tightening; internal generation recovery and URL/data conformance need executable proof; public bytes transport remains open.

## Upward consistency

- Product roadmap/state: exact replacements included.
- Domain rules/target/open/current: included.
- Technical architecture/rules/contracts/open/ADR/indexes: included.
- Requirements: preserved; no edit needed.
- Knowledge packages: not-needed.
- Language gate: passed after remediation.

## Approval

Decision: approved
Applied: 2026-07-15
Approval Source: user decision 2026-07-15 (`Required FIX-001: approve`)
Application Owner: TASK-07.26-0040 / RUN-001 finalization
