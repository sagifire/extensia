# FIX-002: Усунути P4-DG2 domain upward-consistency contradictions

Status: proposed
Requirement: required
Related Task: [P4-DG2 / TASK-07.26-0040](task.md)
Related Run: [RUN-002](RUN-002/index.md)
Source Audit: RUN-001 post-application audit `CHANGES_REQUIRED`

## Мета

Exact corrective proposal усуває лише три contradictions, які approved FIX-001 не охопив. Не застосовано.

## Source preconditions

| Target | Required SHA-256 UTF-8 bytes |
|---|---|
| `memory/domain/rules.md` | `e88988cebd64bb16466a5cb985bd0a3fbd073124ad28536b50f75c76296c922d` |
| `memory/domain/target/model.md` | `e103a20d0b33984c20e8ebde4e2b84737a85828f5d2415c510d05c7f7cb208f4` |
| `memory/domain/open-questions.md` | `35c0ee3a036d13aa51c77e3eef3ee4a11bfa4577d66be295d4993d863d6e47f1` |

Any mismatch або missing/non-unique anchor зупиняє application; ручна адаптація заборонена.

## Exact replacements

### 1. `memory/domain/rules.md`

Replace exact line:

```md
7. Link на asset того самого Resource для `derived_from` є бажаним, але не hard invariant поточного draft.
```

with:

```md
7. `derived_from`, якщо задано, посилається на existing Asset того самого Resource з ready representation, не на self і не створює cycle або dangling relation.
```

### 2. `memory/domain/target/model.md`

Replace exact line:

```md
- `derived_from` є слабким lineage relation до іншого Asset й не змінює ownership.
```

with:

```md
- `derived_from` є hard same-Resource lineage relation до existing Asset з ready representation (external-ready, internal ready або replacement-uploading); self-link, cross-Resource, cycle і dangling relation заборонені; relation не змінює ownership.
```

Replace exact line:

```md
- `Asset 0..1 -> Asset` через weak `derived_from` relation.
```

with:

```md
- `Asset 0..1 -> Asset` через hard same-Resource ready-representation-target `derived_from` relation без self/cycles/dangling links.
```

Replace exact paragraph:

```md
Resource і Asset не мають складних state machines у базовій предметній моделі. Їхній стан задається незалежними flags із кількома обов'язковими combinations. Lifecycle transitions і persistence guarantees є відповідальністю runtime/API layer, а не чистої domain model.
```

with:

```md
Resource не має складної state machine у базовій предметній моделі. Accepted Asset lifecycle має external-ready, initial-uploading, ready і replacement-uploading semantic states; public snapshot flags показують лише external/uploading envelope, а internal durable generation state розрізняє initial-uploading і replacement-uploading. Exact transitions визначає applied P4-DG2 Asset lifecycle, а physical persistence mechanism лишається відповідальністю runtime/driver layer.
```

### 3. `memory/domain/open-questions.md`

Replace exact line:

```md
Updated: 2026-07-10
```

with:

```md
Updated: 2026-07-15
```

Remove exact Resource question:

```md
- Як `updated_at` змінюється для Asset? Для accepted Phase 3 move/delete/Mark/KV усі effective staged Resources мають common operation timestamp; parent не bump-иться лише через derived children projection.
```

Append under `## Закритий P4-DG2 baseline` after its heading:

```md
- Кожний effective Asset/file-state transition використовує common operation Timestamp для changed Assets і affected owning Resources; no-change/failure не змінює persisted timestamps.
```

## Consistency and risk

- Accepted Asset semantics не змінюються; only stale canonical wording is reconciled.
- Current implementation claims не змінюються.
- No index changes: no file/folder structure changes.
- Product/technical/knowledge/state: not-needed.
- Language gate: passed.

## Approval

Decision: pending
Applied: no
Approval Source: pending explicit fixation-specific human decision
