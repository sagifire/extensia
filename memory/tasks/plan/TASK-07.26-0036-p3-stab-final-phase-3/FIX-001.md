# FIX-001: Синхронізувати фактичний current state після Phase 3

Status: applied
Requirement: required
Related Task: [P3-STAB / TASK-07.26-0036](task.md)
Related Run: [RUN-002](RUN-002/index.md)
Approved: 2026-07-12, explicit user decision `Required FIX-001: approve`
Applied: 2026-07-12, exact proposal verified by post-application independent audit `PASS`

## Target files

- `memory/domain/current/implementation-state.md`
- `memory/technical/architecture.md`
- `memory/technical/stack.md`

## Exact proposed changes

### `memory/domain/current/implementation-state.md`

Replace exactly:

```text
As Of: 2026-07-11
```

with:

```text
As Of: 2026-07-12
```

Replace exactly:

```text
- Public opaque full-driver handle є experimental; deterministic fake доводить protocol/recovery behavior, але не concrete physical durability. P3-DG2 semantics, plugins, sync і concrete driver ще відсутні.
```

with:

```text
- `TASK-07.26-0034 / RUN-002` реалізувала exact full-replace Marks і namespace-replace/delete KV через той самий Core/Operation Engine pipeline: descriptor-safe parsing, canonical ordering/equality, accepted limits, one semantic commit/journal entry/fingerprint, prepared batch index publication і detached read-back.
- `TASK-07.26-0035 / RUN-002` реалізувала exact leaf soft delete/default tombstone invisibility: active-parent eligibility, dense sibling reindex, common timestamp, sorted effective write-set, one semantic commit/journal entry/fingerprint, detached tombstone command result і not-found semantics для default reads та інших writes.
- `TASK-07.26-0036 / RUN-002` свіжо перевірила всю Phase 3 matrix: clean install, 20 test files / 202 tests, focused API/semantic/protocol/recovery/concurrency/integrity matrices, package gate і два byte-identical 126-path tarballs. Production correctness defect не знайдено; packed type consumer розширено exact move/delete public types.
- Public opaque full-driver handle є experimental; deterministic fake доводить protocol/recovery behavior, але не concrete physical durability. Restore/include-deleted/cascade/purge/retention, plugins, sync і concrete driver ще відсутні.
```

Replace exactly:

```text
- Фактична структура репозиторію станом на 2026-07-10.
```

with:

```text
- `memory/tasks/plan/TASK-07.26-0034-p3-vs4-mark-kv-writes/RUN-002/result.md`.
- `memory/tasks/plan/TASK-07.26-0035-p3-vs5-resource-soft-delete/RUN-002/result.md`.
- `memory/tasks/plan/TASK-07.26-0036-p3-stab-final-phase-3/RUN-002/result.md`.
- Фактична структура репозиторію станом на 2026-07-12.
```

### `memory/technical/architecture.md`

Replace exactly:

```text
P3-DG2 semantics та concrete driver ще не реалізовані; другого write path немає.
```

with:

```text
Applied P3-DG2 semantics реалізовані для hierarchy/order/move, full-replace Marks, namespace-replace/delete KV і leaf soft delete/default tombstone invisibility через той самий Core/Operation Engine/driver-owned commit/index pipeline. Concrete driver ще не реалізований; другого write path немає.
```

Append exactly after the existing `P3-VS5` Architecture health paragraph:

```text

P3-STAB свіжо перевірила Phase 3 на Node.js `v24.17.0` / npm `11.13.0`: focused API/semantic/protocol/recovery/concurrency/integrity matrices, full 20-file / 202-test package gate і два byte-identical 126-path tarballs зелені. Production correctness defect не знайдено; packed type probe доповнено move/delete public types. Source scan підтвердив один Core write runtime, один driver session boundary, prepared batch index publication і відсутність independent journal runtime/service або реалізованих deferred restore/undelete/plugin/hook/sync paths. Нового architecture pressure чи workaround не додано.
```

### `memory/technical/stack.md`

Replace each of the following four exact table rows independently (the first pair is in `Зберігання`, the second pair is in `API та extensions`):

```text
| Storage Driver contract | current-experimental | Opaque full-driver author boundary і internal Resource session/transaction seams реалізовані для bounded create/update; final wider contract і concrete layout не визначені. |
| Hot Metadata Index | current | Resource-only greedy index має validated immutable batch preparation та atomic publication для create/update/move; lazy mode і wider metadata не реалізовані. |
| Extensia Module | current-bounded | Root `createExtensia` підтримує readonly і opaque experimental full driver; BP3-04/BP3-05 додали bounded Resource create/update та post-commit fail-close без P3-DG2 semantics. |
| Facades | current-bounded | Shared Registry і system `storage`/`query` adapters реалізовані BP2-03 та опубліковані через BP2-04 Module; custom/plugin API ще не реалізований. |
```

with the corresponding row at the same ordinal position below:

```text
| Storage Driver contract | current-experimental | Opaque full-driver author boundary і internal Resource session/transaction seams реалізовані для create/update/move/Marks/KV/leaf delete; final wider contract і concrete layout не визначені. |
| Hot Metadata Index | current | Resource-only greedy index має validated immutable batch preparation та atomic publication для create/update/move/Marks/KV/leaf delete; default get/tree приховують tombstones, lazy mode і wider metadata не реалізовані. |
| Extensia Module | current-bounded | Root `createExtensia` підтримує readonly і opaque experimental full driver; Phase 3 додала bounded Resource create/update/move/Marks/KV/leaf delete та post-commit/integrity fail-close через один pipeline. |
| Facades | current-bounded | Shared Registry і system `storage`/`query` adapters публікують bounded reads та Phase 3 create/update/move/Marks/KV/leaf-delete commands; custom/plugin API ще не реалізований. |
```

Append exactly after the `Resource move slice` row:

```text
| Resource Marks/KV slices | current-experimental | P3-VS4 інтегрувала exact full-replace Marks і namespace-replace/delete KV з canonical ordering/equality, accepted limits, one semantic commit та detached read-back через shared pipeline. |
| Resource leaf delete slice | current-experimental | P3-VS5 інтегрувала active leaf soft delete, dense sibling reindex і default tombstone invisibility через shared hierarchy/write/index pipeline; restore/include-deleted/cascade/purge/retention deferred. |
```

Append exactly after the existing `P3-STAB1/R1` stabilization paragraph:

```text

`P3-STAB/RUN-002` повторно підтверджує всю Phase 3 на Node.js `v24.17.0` / npm `11.13.0`: clean install, 20 test files / 202 tests, focused API/semantic/protocol/recovery/concurrency/integrity matrices, exact root/packed type surface і два byte-identical packs по 126 paths. Production correctness defect не знайдено; packed type probe доповнено move/delete types. Це stabilization evidence, а не concrete durability або cross-environment tar metadata promise.
```

## Rationale

Canonical current/technical memory частково відстає від уже прийнятих TASK-0034/TASK-0035 і свіжих доказів RUN-002. Proposal змінює лише factual current state, не design і не compatibility promise.

## Impact and risks

- Impact: documentation-only factual synchronization.
- Risk: випадково подати deterministic fake як concrete durability; exact changes мають явно зберегти deferred concrete Storage Driver, sync, plugins, restore/include-deleted/cascade/purge/retention.
- Dependencies/versions: без змін.

## Consistency check

- Domain current: included.
- Domain target/rules: not-needed; accepted target contract не змінюється.
- Technical architecture/stack: included.
- ADR/contracts/product: not-needed; design не змінюється.
- Indexes: not-needed; structural change відсутня.
- Language gate: український canonical authoring; code identifiers і команди лишаються англійськими.
