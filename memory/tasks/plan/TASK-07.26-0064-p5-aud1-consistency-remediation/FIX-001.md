# FIX-001: Синхронізувати canonical Phase 5 currentness після P5-STAB/P5-AUD1

Status: applied
Requirement: required
Related Task: [TASK-07.26-0064](task.md)
Related Run: [RUN-001](RUN-001/index.md)
Finding Source: [P5-AUD1 P2-001](../TASK-07.26-0063-p5-aud1-independent-phase-5-audit/RUN-001/result.md)

## Мета

Усунути лише stale currentness anchors, знайдені як `P2-001`, без перепроєктування accepted Phase 5 semantics. Exact application має створити довготривалий запис: P5-STAB accepted/completed; початковий аудит P5-AUD1 у TASK-0063 виявив два зауваження рівня P2. Усунення зауважень фіксує TASK-0064; незалежну повторну перевірку та її результат фіксує TASK-0063. Канонічну підтримку топологій не заявлено до прийнятого P5-AUD1 та явного human gate фази 5; симетрична `full/full` лишається unsupported; designated-writer `full/readonly` — єдиний кандидат, а не supported topology.

Цей proposal не застосовано. Fixation-specific human approval є обов'язковою перед будь-якою mutation canonical targets.

## Normative application payload

Payload застосовується як одна атомарна операція. До першої mutation application має перевірити весь target manifest, усі source hashes, exact occurrence count `old = 1`, `new = 0` для кожного replacement і всі dry-run postimage hashes. Будь-яка невідповідність зупиняє application без partial changes і повертає FIX до review; fuzzy/partial application заборонена.

### Exact target manifest

<!-- VALIDATOR-MANIFEST-START -->
| Target | Required preimage SHA-256 | Expected postimage SHA-256 |
|---|---|---|
| `memory/product/roadmap.md` | `4b9ef7a4eed121e619c8c866565f7cc6a0ddc0db65563fa1087d5b84b5e5a978` | `694089b8c92ca4bbb53825ccee0ad1ae3b1780edb26c61d55b01bbe41cfe2540` |
| `memory/technical/read-model-completeness-contract.md` | `30c9c1a799f8008f41f91b4e74232920d9d8b21a4a9d027fbdacba7c0aa57369` | `d022005b2662ef3243d2091d8bf84c4684eebbbdeadda3cb3eb5e15548af4c12` |
| `memory/technical/open-questions.md` | `4d9c81d147c487b6f3f9b7c50ad4084f84b584687a0c8bc948e0815550a5182c` | `ff380285a131acc80edaa98a3ffe58a485f4a87827b7d9269f10baee0a580b00` |
| `memory/technical/architecture.md` | `4f05e070c74df5e5e05b323ba84d190964813a73dad6d07f4ed9590e2ad85716` | `41cdf4e9b15502c2b16d45be6df37d1a4784fcfd74dd966771a8c4ca4c0150cc` |
<!-- VALIDATOR-MANIFEST-END -->

Target set закритий цими чотирма existing files. Create/delete/rename і будь-які інші canonical changes заборонені.

### A. `memory/product/roadmap.md`

Replace exact one occurrence:

```md
Стан: P5-RS1 accepted; P5-DG1/P5-DG2 exact targets accepted/applied; P5-WP1, P5-HARD1, P5-VS1 і P5-VS2 accepted; TASK-0060/FIX-001 applied, TASK-0061/FIX-001 applied. P5-STAB/P5-AUD1 inactive.
```

with:

```md
Стан: P5-RS1 accepted; P5-DG1/P5-DG2 exact targets accepted/applied; P5-WP1, P5-HARD1, P5-VS1, P5-VS2 і P5-STAB accepted/completed; TASK-0060/FIX-001 і TASK-0061/FIX-001 applied. Початковий аудит P5-AUD1 у TASK-0063 виявив два зауваження рівня P2. Усунення зауважень фіксує TASK-0064; незалежну повторну перевірку та її результат фіксує TASK-0063. Human gate фази 5 і фаза 6 неактивні; канонічну підтримку топологій не заявлено до прийнятого P5-AUD1 та явного human gate фази 5.
```

Replace exact one occurrence:

```md
Wave IDs: `P5-RS1` + `P5-DG1` + `P5-DG2` -> `P5-WP1` generation/coordinator foundation -> `P5-HARD1` internal retry/single-flight/lifecycle -> `P5-VS1` lazy + public explicit refresh/config -> completed `P5-VS2` concrete multi-instance sync/polling/contention -> pending `P5-STAB` -> `P5-AUD1` -> human gate.
```

with:

```md
Wave IDs: `P5-RS1` + `P5-DG1` + `P5-DG2` -> `P5-WP1` generation/coordinator foundation -> `P5-HARD1` internal retry/single-flight/lifecycle -> `P5-VS1` lazy + public explicit refresh/config -> completed `P5-VS2` concrete multi-instance sync/polling/contention -> завершений `P5-STAB` (`full/full` unsupported; designated-writer `full/readonly` — єдиний кандидат) -> початковий аудит `P5-AUD1` у TASK-0063 (два зауваження P2) -> розподіл відповідальності (Усунення зауважень фіксує TASK-0064; незалежну повторну перевірку та її результат фіксує TASK-0063) -> human gate.
```

Prospective Phase 6 text, historical wave identity та інші Phase 5 bullets не змінювати.

### B. `memory/technical/read-model-completeness-contract.md`

Replace exact one occurrence:

```md
`P5-WP1`, `P5-HARD1`, `P5-VS1` і `P5-VS2` materialized та accepted у TASK-0058/0059/0060/0061; відповідні required fixations applied. `P5-STAB` -> `P5-AUD1` лишаються prepared, inactive й потребують separate explicit activation. P5-VS2 acceptance/application не активує downstream packages.
```

with:

```md
`P5-WP1`, `P5-HARD1`, `P5-VS1`, `P5-VS2` і `P5-STAB` materialized та accepted/completed у TASK-0058/0059/0060/0061/0062; відповідні required fixations applied. Початковий аудит P5-AUD1 у TASK-0063 виявив два зауваження рівня P2. Усунення зауважень фіксує TASK-0064; незалежну повторну перевірку та її результат фіксує TASK-0063. Канонічну підтримку топологій не заявлено до прийнятого P5-AUD1 та явного human gate фази 5; симетрична `full/full` лишається unsupported, designated-writer `full/readonly` — єдиний кандидат. Приймання/застосування P5-VS2 не активувало наступні пакети; кожна їх активація потребувала окремого явного рішення owner.
```

Replace exact one occurrence:

```md
P5-VS1 current/materialization sync застосовано через TASK-0060/FIX-001; P5-VS2 current/materialization sync застосовується exact через TASK-0061/FIX-001 у domain current state, architecture, P5 contracts і roadmap. Product requirements, target domain invariants і normative completeness rules не змінюються.
```

with:

```md
P5-VS1 current/materialization sync застосовано через TASK-0060/FIX-001; P5-VS2 current/materialization sync застосовано exact через TASK-0061/FIX-001 у domain current state, architecture, P5 contracts і roadmap. TASK-0064/FIX-001 зберігає канонічний запис актуального стану для accepted/completed P5-STAB і зауважень початкового аудиту P5-AUD1. Усунення зауважень фіксує TASK-0064; незалежну повторну перевірку та її результат фіксує TASK-0063. Product requirements, target domain invariants і normative completeness rules не змінюються.
```

Normative completeness/query/generation semantics та historical design layers не змінювати.

### C. `memory/technical/open-questions.md`

Replace exact one occurrence:

```md
- P5-DG2 accepted target owns supported volatile cursor, legacy `static-unsupported` branch, admission-epoch explicit refresh + opt-in polling, bounded startup/refresh retry, topology gate і one local/external coordinator; implementation/support evidence remains pending.
```

with:

```md
- P5-DG2 accepted target owns supported volatile cursor, legacy `static-unsupported` branch, admission-epoch explicit refresh + opt-in polling, bounded startup/refresh retry, topology gate і one local/external coordinator; докази реалізації матеріалізовано й прийнято у P5-WP1/P5-HARD1/P5-VS1/P5-VS2, а докази стабілізації прийнято й завершено у P5-STAB. Початковий аудит P5-AUD1 у TASK-0063 виявив два зауваження рівня P2. Усунення зауважень фіксує TASK-0064; незалежну повторну перевірку та її результат фіксує TASK-0063. Канонічну підтримку топологій не заявлено до прийнятого P5-AUD1 та явного human gate фази 5.
```

Replace exact one occurrence:

```md
- Initial two-process `full/full`/`full/readonly` candidates remain unsupported until P5 implementation, stabilization, audit and human gate; designated writer recommended.
```

with:

```md
- P5-STAB accepted/completed: симетрична двопроцесна `full/full` лишається unsupported; designated-writer `full/readonly` — єдиний кандидат, а не supported topology. Початковий аудит P5-AUD1 у TASK-0063 виявив два зауваження рівня P2. Усунення зауважень фіксує TASK-0064; незалежну повторну перевірку та її результат фіксує TASK-0063. Канонічну підтримку не заявлено до прийнятого P5-AUD1 та явного human gate фази 5.
```

Replace exact one occurrence:

```md
- Still open: executable support verdict/budgets, arbitrary instance count, retention/compaction, durable checkpoint, notification implementation, multi-host/HA і broader profiles.
```

with:

```md
- Досі відкрито: явне рішення human gate фази 5 щодо підтримки, довільна кількість екземплярів, збереження/компакція, durable checkpoint, реалізація сповіщень, multi-host/HA і ширші профілі.
```

Інші closed baselines та open future items не змінювати.

### D. `memory/technical/architecture.md`

Replace exact one occurrence:

```md
Lifecycle-owned polling стартує тільки після ready, використовує той самий admission-epoch actor, coalesce-ить local cursor-gap wake-up, закриває timer до actor stop і drain-ить observation до driver close. Rerunnable process evidence містить successful lock-wait та lock-to-success retry samples, phase-aligned polling-actor barrier, full/full caller-visible contention і full/readonly zero-write/visibility. Synchronous `DatabaseSync` та rollback-journal contention лишаються architecture pressure; process samples не є fairness/stale-age/SLA certificate, і P5-STAB може звузити support до designated-writer `full/readonly`. Жодна topology не supported до P5-STAB, P5-AUD1 і explicit human gate.
```

with:

```md
Lifecycle-owned polling стартує тільки після ready, використовує той самий admission-epoch actor, coalesce-ить local cursor-gap wake-up, закриває timer до actor stop і drain-ить observation до driver close. Rerunnable process evidence містить successful lock-wait та lock-to-success retry samples, phase-aligned polling-actor barrier, full/full caller-visible contention і full/readonly zero-write/visibility. Synchronous `DatabaseSync` та rollback-journal contention лишаються architecture pressure; process samples не є fairness/stale-age/SLA certificate. P5-STAB accepted/completed і зафіксував: симетрична `full/full` лишається unsupported, designated-writer `full/readonly` — єдиний кандидат, а не supported topology. Початковий аудит P5-AUD1 у TASK-0063 виявив два зауваження рівня P2. Усунення зауважень фіксує TASK-0064; незалежну повторну перевірку та її результат фіксує TASK-0063. Канонічну підтримку не заявлено до прийнятого P5-AUD1 та явного human gate фази 5.
```

P5-VS2 materialization mechanics і architecture-pressure characterization не змінювати.

### Atomic application algorithm

1. Після explicit fixation-specific approval перевести FIX у `Status: approved`, `Decision: approved`, `Applied: no`, а task/run — в application-time `review/finalizing`; canonical targets лишаються unchanged.
2. Запустити `node memory/tasks/plan/TASK-07.26-0064-p5-aud1-consistency-remediation/RUN-001/validate-remediation.mjs` і вимагати `mode: pre`, `status: PASS`.
3. Повторно перевірити всі чотири preimage hashes та occurrence counts безпосередньо перед mutation.
4. У memory застосувати рівно вісім declared old -> new replacements: roadmap `2`, read-model contract `2`, open questions `3`, architecture `1`; до publication перевірити чотири expected postimage hashes, а mismatch має rollback-нути всю application.
5. В тому самому atomic application update встановити FIX `Status: applied`, `Applied: yes` і записати non-pending application evidence з markers `Канонічні заміни застосовано: yes` та `Профіль застосування: exact FIX-001 payload`.
6. Запустити той самий validator з `--post`; вимагати exact postimages, old anchors `0`, new anchors `1`, applied disposition/evidence, UTF-8/link/lifecycle gates `PASS`. Failure rollback-ить canonical replacements і applied disposition.
7. Лише після post `PASS` провести independent post-application audit і TASK-0063 reverification; потім виконати окремі lifecycle/human decisions.

## Verification

- `validate-remediation.mjs` є normative task-local checker manifest/replacements/pre/post hashes.
- Pre-application: exact target set `4`, replacements `8`, source hashes exact, old/new counts `1/0`, dry-run postimages exact.
- Post-application: target hashes exact, old/new counts `0/1`; no undeclared canonical target.
- P2-002 gate: top-level `Status: applied` у TASK-0056 FIX-002/FIX-003.
- UTF-8 without BOM і local Markdown links. Pre mode приймає `active/active + proposed/no`, `review/review-ready + proposed/no` або application-time `review/finalizing + approved/no`; post mode вимагає `applied/yes` із evidence та `review/finalizing` або later `done/completed`.
- Diff-scope, language/upward-consistency й independent audit обов'язкові; production/runtime/package changes заборонені.

## Impact and risks

- Product/technical memory: лише current lifecycle/support truth; normative runtime behavior та API не змінюються.
- Support: не розширюється. `full/full` залишається unsupported; `full/readonly` залишається candidate до accepted P5-AUD1 та explicit human gate.
- Main risk — time-layer collapse або stale preimage. Exact anchors/hashes і atomic stop усувають fuzzy application; substantive mismatch потребує нового reviewed proposal.
- P2-002 уже виправлено operationally й не входить до canonical application payload.

## Upward consistency

- Product roadmap: included, currentness anchors only.
- Technical contract/open questions/architecture: included, currentness/support anchors only.
- Product requirements, domain current/target, ADRs, source specifications, knowledge/project rules: preserved; no edit needed.
- Task/index/progress/state lifecycle: operational, outside fixation payload.
- Production source/tests/package: out of scope, no edit.
- Language gate: Ukrainian author text зі stable identifiers.

## Approval

Decision: approved
Applied: yes
Approval Source: explicit user decision `FIX-001: approve`, 2026-08-23

## Application evidence

Канонічні заміни застосовано: yes
Профіль застосування: exact FIX-001 payload
Applied By: Agent Memory Maintainer / Consistency Remediator
Application Date: 2026-08-23
Scope: рівно 4 declared canonical targets і 8 approved replacements; create/delete/rename відсутні.
Pre-gate: PASS у lifecycle `review/finalizing + approved/approved/no` з exact 4 preimage hashes.
Expected postimages: roadmap `694089b8c92ca4bbb53825ccee0ad1ae3b1780edb26c61d55b01bbe41cfe2540`; read-model contract `d022005b2662ef3243d2091d8bf84c4684eebbbdeadda3cb3eb5e15548af4c12`; open questions `ff380285a131acc80edaa98a3ffe58a485f4a87827b7d9269f10baee0a580b00`; architecture `41cdf4e9b15502c2b16d45be6df37d1a4784fcfd74dd966771a8c4ca4c0150cc`.
Post-validation: PASS, `--post`; exact 4 postimage hashes, 8 new anchors, 0 old anchors, P2-002 `applied/applied`, lifecycle `review/finalizing + applied/approved/yes`, 35 local links.
